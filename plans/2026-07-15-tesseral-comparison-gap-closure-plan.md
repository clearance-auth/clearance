# Tesseral parity and lead plan

## Live execution ledger

- **Status:** In progress
- **Progress:** 22/37 (59%)
- **Branch:** `tesseral-parity-and-lead`
- **Canonical ledger:** `/Users/stephenwalker/Code/projects/clearance-auth/plans/2026-07-15-tesseral-comparison-gap-closure-plan.md`
- **Active goal:** `019f636b-e317-7b42-a47d-0f9f8db7de44`
- **Release target:** `0.3.0`
- **Goal:** Make Clearance demonstrably better than Tesseral on every dimension identified in this comparison while preserving Clearance's existing leads.
- **Completion rule:** Every row below must be `Proven ahead`, with direct source, CLI, live-product, operational, or release evidence recorded in this file. A feature without an API and CLI workflow is incomplete. Completion includes a review-ready pull request followed by the signed `0.3.0` release and public-registry verification.
- **Ledger rule:** Update status, evidence, decisions, dependencies, and next actions after each completed implementation unit. Current code and observed behavior remain the source of truth.

### Status legend

- `In progress`: active implementation work.
- `Queued`: accepted work with unmet dependencies.
- `Lead to protect`: Clearance currently leads; regression proof remains required.
- `Proven ahead`: implemented and verified against the stated acceptance bar.

### Dimension ledger

| Dimension | Acceptance bar for Clearance to lead | Status | Evidence |
|---|---|---|---|
| Normalized persistence and query scale | Normalized relational management data, row-level concurrency, online verified migration, stable cursor queries, and measured 5k/50k performance ahead of the snapshot baseline | In progress | Audit events have atomic reversible relational authority and flat 5k/50k append proof. Commit `3368890` adds reversible principal authority with typed scoped repositories, stable cursor reads, snapshot fencing, relational runtime joins, atomic commit-visible revision/count state, concurrent nonblocking writers, exact migration rollback/re-import, authoritative SCIM apply, public API continuation, external-ID uniqueness, and passing 5k/50k production-path gates. Public transaction callbacks cannot reach raw SQL or physical deletion. Projects, environments, and organizations remain to cut over |
| Credential authentication and authorization | Managed keys authenticate by digest; status, expiry, project, environment, and route scopes are enforced; key identity appears in audit; operator token is bootstrap/break-glass only | Proven ahead | Digest-only authentication, expiry/revocation, key-derived scope, route read/write scopes, operator-only topology/recovery/configuration/tests, audit attribution, CLI expiry, and API-key-aware CLI `whoami`; live CLI proof created an expiring key, authenticated `whoami`, listed users, and rejected `users:write` with exit 1 |
| Durable jobs, email, and webhooks | Transactional outbox, separately deployable worker, retries, dead letters, signed delivery, replay, SES/SMTP, quotas, readiness, and complete CLI control | In progress | Commit `762fc55` makes signup, explicit verification, password reset, invitation create/resend, sign-in/username verification resend, social signup verification, and email-change verification use the durable path; product mutations plus encrypted outbox rows share the ambient Postgres transaction. Commits `d5231d3` and `6a96af8` add schema-v3 fingerprint-key identity, retained-key readiness, rotation-stable generation dedupe, explicit legacy migration, and queued-job processing across rotation. Commit `276f6ec` adds the management PgStore outbox seam and canonical encrypted `organization.updated` delivery intents in the same runtime/management transaction. Commit `2e8ee86` adds exact-byte event-bound HMAC webhook delivery with stable idempotency, HTTPS-by-default endpoints, all-answer global-unicast DNS validation, pinned connections, redirect refusal, bounded DNS/connect/response work, destination-fingerprint verification, and redacted durable outcomes. Commits `b21d89b`, `89c2066`, and `b944ce8` add project/environment-scoped redacted inspection, quotas, operator-only readiness with live managed-key denial proof, transaction-locked retry/cancel/replay previews, leased-cancellation intent, audited mutations, stable API/CLI job-control contracts, strict producer configuration, additive scoped-query indexing, and encrypted managed endpoint storage/execution. Commit `3368890` completes preview-first audited endpoint create/update/rotate/delete/test, active-subscription fanout, canonical URL deduplication, pending/failed/succeeded readiness, one-time secrets, exact API/CLI lifecycle parity, and operator/managed-key authorization. Commit `2cd19f5` adds explicit AWS SES v2 delivery with fixed regional endpoints, SigV4, bounded HTTPS work, non-sending account readiness, stable hashed delivery identity, sanitized provider outcomes, and SMTP-compatible transport-neutral worker health. Commits `c95809f` and `5e942bb` deploy and harden the worker in production Compose and Helm with one API/worker database authority, immutable image references with release-gated signature proof, fail-closed shared key and transport configuration, retained-key validation, bounded graceful drain, concrete resources, least-privilege pod security, destination-scoped DNS/database/SMTP egress, internal readiness/metrics, PDB, and NetworkPolicy-gated ServiceMonitor. Live SES-account proof remains |
| Authentication security | Supported passkeys, TOTP, recovery, factor policy, breached-password defense, account lockout, digest-stored refresh/session secrets, rotating asymmetric access tokens, purpose-separated keys, and KMS providers | In progress | Commit `f871112` productizes bounded breached-password defense, encrypted OAuth credentials, idempotent and replay-safe staged TOTP, encrypted recovery material, a hard concurrent account-attempt ceiling, five-minute Ed25519 access tokens, serialized rotation, bounded verification grace, fail-closed legacy migration, and precise conditional server/client APIs. Commit `158b5da` adds one-way peppered recovery-code digests with online legacy upgrade, lost-response-safe enrollment, exact factor step-up for disable/regeneration, identity-safe account-attempt reservations, storage-aware single-use trusted-device rotation and revocation, factor/session generations that reject late stale inserts and cookie-cache replay, atomic legacy-session backfill, expiry-capped lifecycle sessions, exact public contracts, and real PostgreSQL concurrency proof. Item 21 adds handle-only persisted session and OAuth credentials, peppered digests, atomic refresh-family rotation with lost-response recovery and replay-triggered family revocation, short-lived Ed25519 access-token coupling, signed secondary-storage credential epochs, pre-session OIDC logout, and a database generation fence that rejects stale legacy writers. Exact public `v0.2.1` package compatibility proof passed. Commit `abf3758` adds the passkey ceremony/product foundation: primary-database digest-only one-shot challenges, strict static RP/origin authority, mandatory discoverability and user verification, stable random user handles, real WebAuthn registration and signed authentication, unique credentials, guarded counters transactionally coupled to session issuance, redacted lifecycle views, browser-safe POST option generation, exact conditional server/client types, and Mongo uniqueness authorities. Commits `708647f` and `9f8a22e` close Item 22 with rollback-critical transaction hooks, recent authoritative sessions, distinct surviving passkey/password/TOTP/recovery proof, shared last-viable-factor protection across passkey deletion, credential unlink, and TFA disable, one-shot deletion challenges that remain consumed across ownership misses and later rollbacks, generation compare-and-swap, all-old-session revocation, one expiry-preserving replacement session, exact public APIs, and a fail-closed legacy PostgreSQL bridge. Final proof passed core 12/12, focused runtime 168/168, auth 41/41, real bridge 10/10, PostgreSQL/MySQL/MariaDB/Mongo 5/5, three typechecks, runtime/auth/sample builds, diff hygiene, and a CLEAN Terra-high rereview. Commit `5eb30a3` adds one credential-account password-attempt authority shared by email, username, and phone, with atomic reservations, bounded lock expiry, password-generation fencing, reset-safe settlement, hidden schema fields, exact product defaults, and fail-closed legacy validation. Kysely PostgreSQL, MongoDB, MySQL, MariaDB, and Drizzle PostgreSQL concurrency all passed. Managed factor policy, purpose-separated KMS keys, runtime audit, and exact CLI workflows remain |
| Runtime auditability | Append-only login, recovery, factor, session, SSO, SCIM, API-key, invitation, webhook, and impersonation events with actor, target, outcome, network context, and correlation | Queued | Extend the existing redacted atomic management audit design |
| RBAC and tenant credentials | Canonical many-to-many role/action assignments for people and organization service accounts, consistent runtime action claims, immediate revocation/version behavior, API/CLI/UI parity | Queued | Preserve simple built-in roles while supporting composed assignments |
| Hosted login and customer portal | A production-quality `@clearance/vault` covers login, signup, recovery, organization switching, account security, invitations, enterprise connections, API keys, policy, and audit logs | Queued | Starts after foundational identity workflows stabilize |
| Operator console | Typed nested resource UI with accessible components, generated client, complete operational and product workflows, and exact CLI equivalents | Queued | Preserve current security-focused BFF posture |
| Branding, domains, and enterprise setup | Theme/branding, custom-domain verification, email-domain readiness, templates, provider-guided SAML/OIDC/SCIM setup, testing, and actionable failure ownership | Queued | Builds on Vault, worker, and readiness infrastructure |
| Typed API and SDK onboarding | Runtime-validated canonical contract, generated browser/server clients, precise public types, runnable React/Next/Express paths, then Python/Go/Rust verification SDKs with shared conformance fixtures | Queued | Existing in-tree framework/runtime breadth is the implementation base |
| Observability and image portability | OpenTelemetry across HTTP/Postgres/external protocols/jobs, existing Prometheus retained, multi-architecture application/worker/backup images, anonymous digest-pull proof | In progress | Commit `1c7a048` adds bounded low-cardinality worker claim/outcome/duration/readiness gauges and a Prometheus `/metrics` surface with no tenant, job, event, endpoint, or error labels. Commit `c95809f` exposes that internal surface through a selector-scoped ServiceMonitor and dedicated NetworkPolicy while keeping the worker free of public ingress; OpenTelemetry, multi-architecture, and anonymous digest-pull proof remain |
| CLI and automation | Every workflow is scriptable with stable JSON, noninteractive operation, dry-run/plan where meaningful, structured remediation, and complete API parity | Lead to protect | Existing CLI breadth exceeds Tesseral; add regression/parity proof per new workflow |
| Environment and identity model | Development/preview/production lifecycle and multi-organization principals remain first-class across every new feature | Lead to protect | Existing Clearance model is ahead; every schema and workflow must preserve it |
| SAML and SCIM rigor | Preserve request correlation, replay defense, binding validation, scoped deprovisioning, discovery, session revocation, and fail-closed behavior; close SCIM Groups | Lead to protect | Existing implementation is stronger; SCIM Groups remains queued work |
| Self-hosting, recovery, and release safety | Production Compose/Helm/Terraform, backup/restore/rollback, signed provenance, registry read-back, complete Node matrix, and worker/Vault deployment remain one verified release system | Lead to protect | Existing operations and release gates are ahead; extend them for every new component |

### Active work

- [x] Define and land the storage core for the first reversible store-v2 schema/backfill/verification slice.
- [x] Expose store-v2 plan/status/apply/verify/rollback through the operation registry, API, CLI, and doctor.
- [x] Make managed API keys real `/v1` authenticators with route-scope enforcement and attribution.
- [x] Define the outbox transaction seam against store-v2 so job delivery never races product state.
- [x] Record targeted verification and update dimension statuses for the first mergeable foundation unit.
- [x] Land the first reversible relational-authority slice for audit events.
- [x] Expose audit-event cutover and rollback through API, CLI, and doctor with explicit confirmation.
- [x] Prove the authoritative audit append path at 5k and 50k retained events against the ledger thresholds.
- [x] Cut over principals to normalized relational authority with typed repository writes, reversible migration, cursor reads, snapshot fencing, and 5k/50k production-path proof.
- [x] Close strict-review blockers in the delivery storage and worker core before accepting it.
- [x] Wire signup, verification, reset, and invitation state plus delivery enqueue into one runtime transaction.
- [x] Ship the separately deployable TLS-default SMTP worker with retries, dead letters, lease renewal, provider-acceptance checkpointing, readiness, and graceful drain.
- [x] Version delivery fingerprint keys and prove rotation without losing queued jobs or weakening generation dedupe.
- [x] Add exact-byte signed webhook delivery with event-bound HMAC, idempotency, DNS pinning, redirect refusal, bounded responses, and explicit loopback-only development HTTP.
- [x] Ship scoped delivery inspection, readiness, quotas, retry/cancel/replay controls, and exact CLI/API parity.
- [x] Ship encrypted customer-managed webhook endpoint resources, event subscriptions, secret rotation, test delivery, and exact API/CLI lifecycle control.
- [x] Add SES transport with readiness and failure classification.
- [x] Add delivery observability and deployment manifests.
- [x] Productize bounded breached-password defense, encrypted OAuth credentials, staged replay-safe TOTP, concurrent account lockout, rotating Ed25519 access tokens, legacy-key migration, and precise public APIs.
- [x] Harden recovery and factor lifecycle with one-way recovery codes, step-up disable/regeneration, trusted-device revocation, and session revocation.
- [x] Store session and refresh secrets by digest with rotation, reuse detection, family revocation, and short-lived asymmetric access-token integration.
- [x] Add standards-complete WebAuthn/passkey registration, authentication, discovery, revocation, and recovery-safe policy behavior.
- [ ] Add managed factor policy and password/account lockout with runtime enforcement and API/CLI control.
  - [x] Unit 1: credential-row password-attempt authority shared across email, username, and phone.
  - [ ] Unit 2: durable scoped policy, session assurance, and restricted enrollment with no session-issuance bypass.
    - [x] Unit 2A: immutable runtime scope, uncached reader contract, canonical policy validation, and reserved session authority fields.
    - [ ] Unit 2B: explicit server-derived issuance evidence plus live validation for create, read, rotation, recovery, update, cache, and publication paths.
    - [ ] Unit 2C: digest-only staged enrollment/verification capability with passkey and TOTP completion and no general session before policy satisfaction.
    - [ ] Unit 2D: exact organization binding plus multi-session, one-time-token, device, impersonation, JWT, OIDC, and MCP derivative-credential enforcement.
  - [ ] Unit 3: product PostgreSQL policy authority and verified migration.
  - [ ] Unit 4: revisioned get/plan/apply/unlock API and CLI workflows.
- [ ] Separate credential purposes and add AWS/GCP KMS-backed signing/encryption providers with local development parity.
- [ ] Add append-only runtime authentication audit events and exact API/CLI inspection and mutation workflows for every security feature.

### Execution log

| Date | Change | Evidence / next action |
|---|---|---|
| 2026-07-15 | Created branch `tesseral-parity-and-lead` and activated the plan-linked goal | Goal `019f636b-e317-7b42-a47d-0f9f8db7de44`; begin credential enforcement and store-v2 foundation |
| 2026-07-15 | Started managed API-key implementation lane | Inspect live authentication middleware and add narrowly scoped tests; manager verification required |
| 2026-07-15 | Started read-only store-v2 implementation-slice review | Select a reversible first slice that preserves the existing JSON backend and avoids speculative rewrites |
| 2026-07-15 | Set terminal release target to `0.3.0` | Open a review-ready PR after implementation and complete proof; release only after approval and the full release gate |
| 2026-07-15 | Accepted store-v2 Slice 1 boundary | Normalize projects, environments, principals, and organizations behind explicit apply; snapshot stays authoritative during verified shadow mode; storage implementation started |
| 2026-07-15 | Accepted delivery-plane dependency boundary | Runtime email outbox may precede store-v2; management webhook emission waits for transactional delivery intents; encrypted payloads and signed HTTPS delivery are mandatory |
| 2026-07-15 | Implemented managed API-key authentication | Full API suite 88/88, focused auth/security 5/5, management key lifecycle 6/6, CLI auth and transport 18/18; API, management, and CLI typechecks green |
| 2026-07-15 | Proved managed-key operation through the live API and CLI | CLI created an expiring scoped key, `whoami` identified the key and exact scopes, `users list` succeeded, and `users create` failed with `API_KEY_SCOPE_FORBIDDEN` and exit 1; live proof artifacts moved to Trash |
| 2026-07-15 | Landed store-v2 Slice 1 storage core | Pure helpers 4/4; canonical real-Postgres regression 34/34 across shadow, revision drift, atomic swaps, environment reparenting, concurrency, user lifecycle, and organization lifecycle; management typecheck and diff check green |
| 2026-07-15 | Hardened store-v2 adoption boundaries during manager review | Apply refuses unowned target-table collisions, invalid phase metadata, and future schema versions without touching external data |
| 2026-07-15 | Completed store-v2 control-plane integration | Stable registry/API/CLI envelopes for status, plan, apply, verify, and rollback; two-sided confirmation, doctor visibility, structured failure, operation contracts 9/9, API operations 5/5, CLI transport 11/11 |
| 2026-07-15 | Closed two strict security reviews on the foundation | Fixed missed-writer revision masking, valid unique swaps and environment reparenting, forged enterprise audit actors, raw settings exposure, expired rotation and dry-run, case-variant staging, and SCIM SSRF using all-answer validation, global-unicast policy, pinned DNS, redirect refusal, and operator-only enterprise probes; final review reported no blocker |
| 2026-07-15 | Committed the first mergeable foundation unit | Commit `ca361e7` contains managed API authorization, store-v2 shadow/control plane, strict-review hardening, live-ledger evidence, and no live secret material |
| 2026-07-15 | Selected first relational-authority cutover | Audit events move first into a hybrid authoritative store: immutable append cost becomes O(1), the JSON backend remains unchanged, rollback reverse-materializes the visible projection, and 5k/50k production-path append benchmarks gate acceptance; storage implementation started |
| 2026-07-15 | Froze the delivery-plane transaction and security boundary | Runtime email only in Slice 1; product state plus immutable event, encrypted payload, and initial job must use the ambient runtime adapter transaction; no pool, second Kysely instance, background helper, or post-commit hook; management webhooks remain deferred |
| 2026-07-15 | Started delivery storage/worker-core implementation | New `@clearance/delivery` package is limited to guarded schema migration, purpose-specific AEAD/HMAC keyring, transaction-adapter enqueue, fenced leasing/retry/dead/cancel/reclaim, retention crypto-erasure, redacted views, and Postgres proof; SMTP/runtime/API/CLI/deploy wiring follows after core review |
| 2026-07-15 | Completed the audit-event relational-authority core (`1431e88`) | Hybrid cutover empties the snapshot event projection, relational appends and resource mutations share one transaction, cursor reads are stable, retention hides historical rows instead of deleting them, and rollback reverse-materializes the visible projection; management typecheck, focused unit 7/7, and canonical real-Postgres 35/35 passed |
| 2026-07-15 | Rejected the initial delivery core at strict review | Active transactions, tenant-scoped dedupe, semantic-expiry completion, canonical verification inclusion, lease-owner binding, safe erasure, channel-bound AAD, schema-drift detection, structural retention, and provider-value redaction must close before commit |
| 2026-07-15 | Completed audit-event authority control-plane integration (`10646f6`) | Added confirmed API and CLI cutover/rollback operations, operation-registry coverage, and hybrid-authority doctor evidence; management contracts 9/9, API operations 5/5, CLI transport 11/11, and all three package typechecks passed |
| 2026-07-15 | Accepted hardened delivery storage and worker-facing primitives (`5c8b2a9`) | Strict-review blockers closed: active transaction savepoint enforcement, tenant-scoped dedupe, channel-bound AEAD, owner-fenced leases, semantic-expiry dead-lettering, atomic erasure, bounded retention, schema-shape verification, opaque provider metadata, and fail-closed canonical Postgres/CI inclusion; delivery build and five-package typecheck passed; canonical Postgres gate passed management 250/250 and delivery 9/9 with zero skips |
| 2026-07-15 | Proved authoritative audit scale on the production PgStore path (`9ff5667`) | Manager rerun with five warmups and 50 samples: 5k p50/p95 6.35/6.75ms; 50k p50/p95 8.29/9.78ms; 50k flatness gate 9.78 <= 18.50ms; snapshot event projection remained 2 bytes with zero growth; normalized persistence remains in progress pending remaining collection cutovers |
| 2026-07-15 | Completed atomic runtime email enqueue (`762fc55`) | Signup hashes and creates its verification generation before opening the real durable transaction; user, credential account, session, and outbox mutations then commit together; reset token, invitation create/resend, email changes, and unverified social signup share their enqueue transaction; explicit and sign-in/username verification resend use the same durable contract; legacy configurations retain callback behavior; four real-Postgres proofs cover five committed jobs, all-five worker processing, enqueue-failure rollback, and later-mutation/hook rollback |
| 2026-07-15 | Completed the first deployable SMTP worker (`762fc55`) | TLS or STARTTLS is mandatory except explicit unauthenticated loopback development; canonical verification/reset/invitation rendering is bounded and HTML-escaped; destination fingerprints are checked before send; leases renew during slow providers; accepted outcomes are durably checkpointed and ambiguous accepted jobs dead-letter instead of intentional resend; readiness rechecks DB, owned schema v2, required tables, SMTP, keyring, and heartbeat; drain timeout force-closes resources and returns failure |
| 2026-07-15 | Proved the integrated delivery slice through the canonical Postgres gate | Manager rerun: management 250/250, delivery 12/12, delivery-worker 14/14, and clearance-auth 22/22, all with machine-asserted zero skips; six affected packages typecheck and the three publishable packages build; focused affected-runtime verification passed 199/200 with one pre-existing declared skip, wrapper 8/8, and core transaction 4/4; `git diff --check` passed |
| 2026-07-15 | Recorded strict-review follow-up for key rotation and control plane | Before the delivery dimension can be `Proven ahead`, fingerprint keys must be versioned and retained across queued jobs, and scoped/redacted API plus CLI controls must land atomically with immutable operator audit and quota enforcement |
| 2026-07-15 | Completed delivery fingerprint-key rotation (`d5231d3`, `6a96af8`) | Schema v3 records source and destination fingerprint key IDs, keeps a purpose-separated stable generation-dedupe authority, transactionally upgrades owned v1/v2 data only with an explicit retained legacy key ID, refuses premature key retirement, processes queued jobs created under retained old keys, and rechecks key availability on every worker readiness probe |
| 2026-07-15 | Completed the management transactional delivery-intent seam (`276f6ec`) | PgStore exposes an opaque enqueue closure bound to its active transaction; canonical `organization.updated` intents carry project/environment/organization/actor/correlation attribution while URL and signing secret remain encrypted; real Postgres proof covers commit, enqueue-failure rollback, later-mutation rollback, concurrent dedupe, environment isolation, and fail-closed unconfigured stores |
| 2026-07-15 | Made the canonical PostgreSQL gate concurrency-safe (`2f1f8eb`) | Per-process container names prevent parallel verification runs from deleting each other's databases; the accepted rerun passed management 257/257, delivery 13/13, delivery-worker 15/15, and clearance-auth 22/22 with machine-asserted zero skips |
| 2026-07-15 | Accepted the next delivery dependency boundary | Build signed exact-byte HTTPS webhook transport and endpoint hardening first, then scoped/redacted API and CLI inspection/retry/cancel/replay/readiness, quotas, SES, observability, and deployment manifests |
| 2026-07-15 | Completed signed webhook transport (`2e8ee86`) | Canonical event bytes are signed and sent unchanged; signatures bind event ID and timestamp, stable job IDs provide idempotency, endpoint credentials stay inside the encrypted payload, all DNS answers must satisfy the global-unicast policy, the selected address is pinned while preserving TLS hostname validation, redirects are terminal, and DNS/connect/response bytes/time are bounded. The invalidated first gate exposed and fixed an oversized-response settlement race; the accepted rerun passed management 257/257, delivery 13/13, delivery-worker 24/24, and clearance-auth 22/22 with zero skips. Next: scoped/redacted controls and quotas |
| 2026-07-15 | Completed scoped delivery operations (`b21d89b`) | Added redacted project/environment list and inspect, scoped quota status, operator-only global readiness, preview-first audited retry/cancel/replay, strict JSON and CLI option parsing, exact operation-registry/API/CLI parity, validated producer key/quota configuration, and a repairable scoped event index. API passed 94/94, focused CLI 21/21, four package typechecks passed, and the accepted canonical Postgres rerun passed management 267/267, delivery 18/18, delivery-worker 24/24, and clearance-auth 22/22 with machine-asserted zero skips. Next: customer-managed endpoint resources and SES transport |
| 2026-07-15 | Hardened delivery route authorization proof (`89c2066`) | Added live managed-key denial proof for operator-only global readiness and removed a redundant post-commit flush because coordinated delivery mutations already resolve after commit; focused API 11/11 and API typecheck passed |
| 2026-07-15 | Closed replay-preview contract mismatch (`adcba44`) | Replay previews now validate and display the requested `maxAttempts` override that confirmed execution will use; delivery and management typechecks, focused management 3/3, API 6/6, and live-Postgres control 5/5 passed |
| 2026-07-15 | Selected principal relational authority as store-v2 Slice 2 | Principals are the smallest dependency-correct high-cardinality cutover that removes the measured principal-create snapshot rewrite and rebuilt email index. The slice requires typed transaction-bound reads/writes, `principals: []` snapshot fencing, authority revision refresh, explicit cutover/rollback controls, real cursor reads, and 5k/50k gates of p50 <25ms, p95 <50ms, and 50k p95 <= 2x 5k p95 + 5ms; events remain relational-authoritative while projects, environments, and organizations remain shadowed |
| 2026-07-15 | Completed AWS SES transport (`2cd19f5`) | Explicit `ses` selection uses the SES v2 HTTPS API at a validated fixed regional host, deterministic SigV4, static or session credentials from Clearance/AWS environment names, bounded timeout and 64 KiB responses, redirect refusal, non-sending `GetAccount` readiness, stable hashed delivery headers, hashed provider IDs, and sanitized retry/permanent classifications. SMTP remains the default and passed its real compatibility gate. Worker build and four affected typechecks passed; the accepted canonical gate passed management 267/267, delivery 18/18, delivery-worker 28/28, and clearance-auth 22/22 with zero skips. Live SES-account proof remains required in a credentialed release environment |
| 2026-07-15 | Froze customer-managed webhook endpoint contract | Delivery schema v4 will add normalized project/environment endpoint resources with purpose-bound encrypted URL/secret configuration, one-time generated secret output, exact closed event subscriptions, optimistic versions, crypto-erasing soft delete, endpoint provenance on immutable events, preview-first rotate/delete/test, atomic audit/enqueue, and exact scoped API/CLI parity. Runtime DNS and pinned HTTPS validation remains mandatory on every attempt; create-time validation never becomes durable network trust |
| 2026-07-15 | Added delivery-worker Prometheus foundation (`1c7a048`) | Worker claims, bounded outcomes, cumulative processing duration, in-flight work, drain state, schema health, email-transport health, and uptime are exposed at `/metrics` without high-cardinality or sensitive labels. Focused unit tests 11/11, worker typecheck/build, and the schema-v4 real-Postgres worker rerun 22/22 passed; Compose/Helm deployment and end-to-end telemetry remain under review |
| 2026-07-15 | Deployed the transactional worker in production Compose and Helm (`c95809f`) | API and worker share fail-closed keyring, quota, schema, and transport configuration; production uses immutable digest images, a 310-second Compose stop grace, Helm grace/drain validation, hardened pod security, PDB, internal health/metrics Service, selector-scoped ServiceMonitor, and dedicated DNS/database/HTTPS plus destination-scoped SMTP egress. Manager verification passed Helm lint/default, SMTP, SES, metrics, maximum integer rendering, unsafe egress/path rejection, production environment validation, resolved Compose hardening, and `git diff --check`. Next: endpoint lifecycle and full delivery integration gate |
| 2026-07-15 | Closed strict deployment review (`5e942bb`) | Worker database configuration now comes from the API Secret authority; IPv4/IPv6 allow-all SMTP CIDRs, unscoped DNS/database egress, insecure SMTP, ServiceMonitor without NetworkPolicy, unknown retained fingerprint IDs, and empty resources fail closed. Digest syntax is described accurately while signature/provenance remains a terminal release proof. Independent focused operations verification passed every static, environment, Compose, default/SMTP/SES Helm, and adversarial render check through the pre-Postgres section with zero failures; full backup/restore proof remains part of the release gate. |
| 2026-07-15 | Accepted managed endpoint storage and worker execution (`b944ce8`) | Schema v4 adds project/environment-scoped encrypted endpoint config, retained-key readiness, strict constraints/index drift verification, optimistic versions, active-subscription fanout, destination provenance, one-time generated secrets, canonical URL views, and crypto-erasing delete with queued cancellation plus leased cancellation requests. Exact `webhook.endpoint.test` payloads are transactionally enqueued, event-bound HMAC signed, and delivered through the pinned HTTPS path; managed enqueue/fanout/test/replay honor configured quotas and port 443 matches production egress. Manager rerun passed delivery 28/28, delivery-worker 31/31, both typechecks, and diff checks against live PostgreSQL. Next: preview-first audited management lifecycle with exact API/CLI parity. |
| 2026-07-15 | Completed principal relational authority (`3368890`) | Typed project/environment-scoped writes and relational reads now cover user lifecycle, sessions, members, export, migration, SCIM, and runtime identity sync. A transactionally locked `{revision,count}` state publishes only committed counts, staggered cross-store writers refresh to the authoritative SQL count, public callbacks receive a frozen allowlisted repository with no raw SQL, rollback physical deletion requires an internal exact checkpoint, failed mutations leave revision unchanged, and rollback/re-import succeeds. Principal PG and 5k/50k scale tests passed 2/2; the strict reviewer closed cache coherence and rollback-capability exposure. Remaining persistence work: projects, environments, and organizations. |
| 2026-07-15 | Completed managed webhook endpoint lifecycle (`3368890`) | Preview-first audited create/update/rotate/delete/test, one-time secret output, exact subscriptions, optimistic versions, active fanout, canonical URL dedupe across managed and legacy targets, crypto-erasing deletion, and pending/failed/succeeded test readiness now have management/API/CLI parity. Strict review closed queued-test false readiness and double-delivery risk. Delivery passed 28/28 on required PostgreSQL; API passed 100/100; CLI passed 94/94. Live SES-account proof is the remaining delivery-dimension gate. |
| 2026-07-15 | Accepted integrated principal and endpoint proof | Required-Postgres management passed 287/287; focused principal/delivery/concurrency/rollback proofs passed 35/35; management built; management, API, CLI, and delivery typechecks passed; internal rollback/SQL capabilities were absent from public build exports; `git diff --check` passed. Next: authentication-security foundation in dependency order. |
| 2026-07-15 | Completed the authentication-security foundation (`f871112`) | Product defaults now wire bounded HIBP checks, encrypted OAuth tokens, idempotent encrypted TOTP enrollment, old-factor-proven staged replacement, atomic timestep replay defense, a hard concurrent account-attempt ceiling, rotating five-minute Ed25519 tokens, public-only JWKS, bounded immutable legacy verification grace, and conditional typed server/client APIs. Runtime passed 157/157; product passed 14/14; required Postgres passed 4/4 including repeated-migration retirement, one-row factor authority, concurrent rotation, encrypted private keys, and legacy schema upgrade; runtime/auth typechecks and utils/runtime/auth builds passed; final strict review found no P0-P2. Next: one-way recovery and step-up factor lifecycle. |
| 2026-07-15 | Completed recovery and factor lifecycle hardening (`158b5da`) | Recovery codes are one-way HMAC digests under an encrypted per-enrollment pepper with exact-one CAS consumption, online plaintext/encrypted legacy upgrade, malformed-envelope fail-closed behavior, and restartable lost-response enrollment. Disable, regeneration, activation, and replacement require exact factor proof, rotate trust/session generations, revoke sessions and trusted devices across database or secondary storage, reject stale TOTP proofs and sessions inserted after deletion, preserve the original absolute session expiry, and expose exact typed client/server contracts. Per-attempt lockout identities refund only their own internal-error reservation. Runtime two-factor passed 101/101; session, internal-adapter, and cookie-cache regression passed 127/127; product and required PostgreSQL passed 15/15; runtime/auth typechecks and builds passed; `git diff --check` passed; final strict rereview found no P0-P2. Next: digest-stored session and refresh families with rotation, reuse detection, and asymmetric access-token integration. |
| 2026-07-16 | Completed session, refresh-family, and OAuth credential authority | Persisted credentials expose only random handles and peppered digests; refresh rotation is atomic, lost-response-safe, and revokes a reused family; short-lived Ed25519 access tokens stay coupled to credential state; secondary storage carries a signed credential epoch; OIDC logout works before session materialization; and the `legacy-v1` to `digest-v1` fence drains, migrates, verifies, and rejects stale writers. Exact public `v0.2.1` artifact proof passed. Runtime passed 921 tests with 9 expected PostgreSQL skips; focused/broader regressions passed 207/207 and 465/465; database, API/CLI, and production-ops gates passed; three package typechecks/builds passed; package-surface proof passed 245/245 with a CLEAN independent Sonnet verdict at frozen SHA `5c4c1ce6e9631a650a245787225be7bdbab1bf34045de965003d9f59aa97847a`. Next: standards-complete passkeys. |
| 2026-07-16 | Froze the passkey security boundary and changed delegated-worker routing | All new delegated lanes use Claude Sonnet 5 under manager-authored, path-bounded prompts; the managing agent retains architecture, integration, verification, ledger, and commit ownership. Item 22 reuses Clearance's database-backed atomic challenge consumption and guarded adapter updates, requires configured origin/RP trust, mandatory user verification, unique credential IDs, discoverable login, redacted lifecycle views, counter compare-and-swap, and recovery-safe revocation. The first implementation slice is the runtime ceremony core; product wiring and destructive lifecycle remain separate reviewable slices. |
| 2026-07-16 | Closed the final Sonnet lane and changed future worker routing to Terra | The final read-only Sonnet audit was interrupted after it wrote outside its permission boundary and restored raw WebAuthn challenge persistence; that change was rejected and removed. Every new delegated engineering lane now uses `gpt-5.6-terra` at high reasoning with manager-authored bounded scope and targeted local verification. The managing Sol agent continues to own architecture, integration, builds, source-of-truth review, ledger updates, commits, and final judgment. |
| 2026-07-16 | Completed the passkey ceremony and product foundation (`abf3758`) | Clearance now ships default-on, conditionally typed passkey server/client APIs backed by `@simplewebauthn`; strict static RP/origin resolution; POST-based browser ceremonies; digest-only, typed, expiring, atomically consumed primary-database challenges with opportunistic cleanup; mandatory resident credentials and user verification; stable non-correlating user handles; discoverable authentication; globally unique credential IDs; redacted list/rename views; and counter compare-and-swap plus session creation in a required rollback-capable transaction. PostgreSQL fault injection proves a session created before a later failure rolls back together with the counter. Mongo installs verified remapped unique authorities for user handles, credentials, and challenge digests. Focused runtime passed 72/72, product 3/3, Mongo adapter 15/15, real PostgreSQL/MySQL/MariaDB/Mongo 4/4, rendered-browser Origin proof 1/1, runtime/auth/Mongo typechecks and runtime/auth builds passed, direct package import smoke passed, and the final Terra-high rereview was CLEAN. Item 22 stays open. Next: recent-proof passkey deletion and compromise revocation, last-viable-factor protection, and passkey session-generation invalidation. |
| 2026-07-16 | Accepted transactional passkey session-generation fencing (`0472a82`) | User and session rows now carry hidden passkey generations; DB-backed creation locks the user and initializes, backfills, hook-protects, revalidates, and inserts both passkey and two-factor generations in one rollback-capable transaction. Secondary-authoritative legacy sessions remain valid while both values are absent, then fail closed after lifecycle authority is established; malformed or mismatched generations, stale refresh rotation/recovery, direct ID reads, lists, and signed cookie-cache replay are rejected. Stale post-commit dual-write cache publication is skipped without misreporting an already committed DB session. Terra-high review found and drove closure of seven P1/P2 race, migration, rollback, and proof defects; manager verification caught and fixed one generic secondary-storage regression. Runtime typecheck passed, focused passkey/two-factor passed 27/27, the seven-file session/cookie/secondary/credential regression gate passed 289/289, runtime built, `git diff --check` passed, and the final Terra-high rereview was CLEAN. Item 22 stays open. Next: distinct-surviving-factor deletion proof, atomic credential removal, generation rotation, all-old-session revocation, and exactly one expiry-preserving replacement session. |
| 2026-07-16 | Completed recovery-safe passkey and factor lifecycle (`708647f`, `9f8a22e`) | Rollback-critical hooks now execute before commit and fail closed, custom mutations receive the ambient transaction adapter, and post-commit publication failures are aggregated without misreporting committed work. Passkey deletion, credential unlink, and TFA disable share authoritative-session checks, last-viable-factor protection, generation CAS, all-old-session revocation, and one expiry-preserving replacement session. Passkey deletion requires a distinct surviving factor; its digest-only challenge is consumed before ownership lookup and stays consumed across foreign/nonexistent targets, invalid proofs, and later lifecycle rollback. The legacy PostgreSQL bridge installs and validates exact columns, constraints, and indexes, rejecting partial, invalid, expression, included-column, unready, non-live, and `NULLS NOT DISTINCT` authorities with DDL rollback. Core passed 12/12, focused runtime 168/168, auth 41/41, real PostgreSQL bridge 10/10, cross-adapter PostgreSQL/MySQL/MariaDB/Mongo 5/5, core/runtime/auth typechecks and runtime/auth/sample builds passed, `git diff --check` passed, and the strict Terra-high rereview was CLEAN. Large legacy PostgreSQL tables require a maintenance window because bridge alterations and non-concurrent index creation can hold locks. Item 22 is complete. Next: Item 23 managed factor policy and account lockout. |
| 2026-07-16 | Froze the Item 23 dependency boundary | Current source proves concurrent durable second-factor lockout exists, while password sign-in has no shared budget across email, username, and phone; security configuration is startup-only and no managed policy authority reaches runtime replicas. Item 23 will land in four dependency-ordered units: credential-row password-attempt authority; durable scoped policy plus session assurance and restricted enrollment; product PostgreSQL authority and migration; then revisioned plan/apply/get/unlock API and CLI control. A `required` factor flag cannot ship before restricted enrollment and every session-issuance bypass are enforced. Unit 1 is active under a bounded Terra-high implementation contract; manager source review, builds, integration, and acceptance remain required. |
| 2026-07-17 | Completed shared password-attempt authority (`5eb30a3`) | Credential-account rows now own a default-on 10-attempt/15-minute password budget shared across email, username, and phone. Reservation CAS occurs before attacker-controlled hash work; concurrent success, failure, expiry, policy-threshold reduction, and password replacement settle without stale relock or old-password session issuance. Drizzle repeats the full guard on the outer PostgreSQL update. Hidden fields and exact legacy defaults fail closed. Manager proof passed focused runtime 158/158, product 10/10, legacy PostgreSQL 11/11, real Kysely PostgreSQL/MongoDB/MySQL/MariaDB plus Drizzle PostgreSQL 5/5, core/runtime/auth/Drizzle typechecks, five package builds, diff hygiene, and a CLEAN Terra-high rereview. Item 23 stays open; next is durable scoped policy, session assurance, and restricted enrollment. |
| 2026-07-17 | Froze the complete Unit 2 enforcement map | Every production session issuer converges on `internalAdapter.createSession`, while rotation/recovery, cookie cache, multi-session, one-time-token, organization switching, device exchange, impersonation, JWT, OIDC, and MCP can republish or derive authority afterward. Managed policy therefore binds an immutable product project/environment identity to an uncached scope-validating reader, accepts only runtime-derived evidence, stamps hook-proof session authority, and re-reads live policy before every issuance, publication, refresh, switch, or protected organization use. An unmet policy issues only a short-lived digest-stored, stage-bound enrollment/verification capability; it never creates a general session. Unit 2 owns the reader contract, assurance engine, restricted capability, derivative-path enforcement, and stale lock-reservation fencing with adversarial fake-reader proof. Unit 3 owns normalized product PostgreSQL tables, migrations, single-statement scoped reads, and revision concurrency. Unit 4 owns revisioned management/API/CLI policy and unlock workflows. Implementation starts with Unit 2A. |
| 2026-07-17 | Completed managed policy and session-assurance contracts (`79507d8`) | Unit 2A now provides one-time immutable project/environment binding, an uncached captured reader, exact same-read subject/organization membership attestation, canonical positive PostgreSQL-bigint revisions, strict plain-data policy and override normalization, recomputed effective policy, and opaque issuance contexts. Eleven runtime-owned session authority fields are migration-safe, hidden from public inference and output, protected from plugin/options replacement, and denied by generic create/update types. The assurance engine derives only from explicit evidence or a module-private immutable source snapshot; it enforces passkey, TOTP, OTP, recovery, impersonation, trusted-device, scope, organization, revision, freshness, causal timestamp, and expiry invariants without accepting caller labels or mutable `Date` authority. Manager verification passed core 12/12, focused runtime 65/65, core/runtime typechecks and builds, formatting, and diff hygiene; reciprocal Terra-high rereviews were CLEAN after closing membership, binding-mutation, prototype-injection, reserved-write, mutable-proof, device-cap, and stale-factor findings. Next: Unit 2B central issuance and live session-path enforcement. |

### Current implementation decisions

- Store-v2 is default-off and Postgres-only. Explicit plan/apply/status/verify/cutover/rollback operations control adoption; events and principals can become independently relational-authoritative while projects, environments, and organizations remain verified shadows.
- Relational principal authority publishes a compact atomic `{revision,count}` state only at transaction finalization. Public callbacks receive frozen allowlisted capabilities without raw SQL or physical deletion; divergence fails verification without exposing PII.
- Audit and principal authority remove their snapshot rewrite costs and pass 5k/50k production gates. The persistence dimension stays `In progress` until projects, environments, and organizations complete equivalent reversible cutovers.
- Delivery uses Postgres-native immutable events, leased jobs, attempts, and worker heartbeats. Payloads and endpoint secrets are encrypted under a purpose-specific encryption keyring. Schema v3 records fingerprint key identity, preserves a separate stable dedupe authority across rotations, requires explicit legacy-key attribution for non-empty v1/v2 migration, and blocks workers from becoming ready while any queued/leased/retry destination or migrated legacy source authority references an unavailable key.
- Customer-managed webhook endpoints live in guarded delivery schema v4 rather than the management snapshot. URL plus signing secret use a purpose-derived encryption subkey and scope/version-bound AAD; public views omit envelopes and key IDs, create/rotate reveal a generated secret once, exact subscriptions start with `organization.updated`, URL changes disable the endpoint, and versioned lifecycle mutations fail closed on stale writes.
- Runtime verification, reset, and invitation email jobs use their existing relational transaction context. Management PgStore mutations can issue opaque delivery enqueues on the same transaction; `organization.updated` is the first canonical management event. Scoped delivery jobs and managed endpoint lifecycle are stable operation-registry resources with exact API/CLI parity.
- Delivery semantics are at-least-once with stable event IDs, deterministic SMTP Message-ID values, bounded full-jitter retries, and dead-letter state. Provider acceptance is checkpointed before final completion when the lease remains owned; the unavoidable crash boundary after provider acceptance remains an at-least-once ambiguity. Webhook signatures cover the exact canonical bytes plus event ID and timestamp; HTTPS, all-answer global-unicast validation, connection pinning, redirect refusal, bounded response handling, and destination-fingerprint verification fail closed. Retry/cancel/replay and endpoint lifecycle are preview-first, explicitly confirmed where destructive, scoped, audited, and available through API and CLI.
- Delivery-worker metrics use a fixed label vocabulary only: transport channel, bounded outcome, and configured email transport. Tenant, project, environment, endpoint, event, job, provider response, and error values never become metric labels. `/metrics` remains an internal operational surface alongside JSON `/live` and `/ready`.
- Runtime delivery uses a distinct durable enqueue capability. Signup uses its existing adapter transaction; reset and invitation state changes are wrapped with `runWithTransaction` and enqueue through `getCurrentAdapter`. Legacy background email callbacks and `queueAfterTransactionHook` are prohibited because they leave post-commit loss windows.
- Delivery persistence separates immutable event metadata, encrypted expiring payloads, jobs, append-only attempts, and worker heartbeats. Recipient, subject, body, URLs, reset/verification tokens, and invitation details remain inside a purpose-specific AES-256-GCM envelope; keyed fingerprints support quotas and deduplication without plaintext PII.
- Relational audit retention is logical: rows removed from the visible retained window are tombstoned with `visible = false`, preserving the append-only archive. Rollback materializes the visible projection into the snapshot and requires a fresh verified cutover before relational authority resumes.
- Authentication security defaults are product-owned: TOTP, passkey ceremony support, and five-minute Ed25519 access tokens are enabled unless explicitly disabled; HIBP is enabled in strict/production mode; OAuth provider tokens use versioned authenticated envelopes; and public API types reflect disabled or dynamically configured plugins. Factor enrollment is one-row and lost-response-safe, replacement requires the active factor and stages until proof of the new secret, TOTP timesteps are consumed atomically against exact factor generation, and identity-bearing account attempt reservations precede attacker-controlled comparisons. Recovery credentials use one-way digests with online legacy upgrade; disable and regeneration require exact TOTP or recovery proof and rotate storage-aware trust plus session generations. Session and OAuth credential authority persists only handles plus peppered digests, rotates refresh families atomically with lost-response recovery and reuse revocation, binds short-lived access tokens to current authority, signs secondary-storage epochs, and fences `legacy-v1` writers before verified `digest-v1` cutover. Legacy signing keys retain their first retirement timestamp so repeated migration cannot extend grace. Policy, KMS, runtime audit, and exact CLI control remain open gates.
- Passkey ceremonies use a reviewed WebAuthn library and Clearance's primary database for typed, short-lived, single-use challenges. Request headers never become RP or origin authority; discoverable credentials remain enumeration-resistant; credential identifiers, public keys, user handles, and counters stay internal; and nonzero signature counters advance through a guarded compare-and-swap before session issuance. Destructive passkey, password, and TFA lifecycle operations require an authoritative current session and a surviving factor, consume one-shot proof outside destructive rollback, rotate generation authority with compare-and-swap, revoke every old session, and issue exactly one replacement capped to the original expiry. Legacy PostgreSQL bridge DDL fails closed on incompatible column, constraint, or index authority; large installations must schedule its table alterations and non-concurrent index creation within a maintenance window.
- Managed authentication policy will use normalized, revisioned PostgreSQL authority scoped to Clearance project and environment, with optional organization overrides validated against that scope and uncached runtime reads. Password attempt authority now belongs to each credential account and is shared across email, username, and phone entry points; product defaults are enabled, 10 attempts, and 15 minutes, while direct runtime configuration is bounded and legacy state fails closed. Required-factor policy depends on durable session assurance and a restricted enrollment flow; policy activation cannot silently exempt unenrolled users or trust client-supplied assurance.
- Policy-enabled runtimes capture immutable project/environment identity separately from delivery configuration, environment variables, request headers, and active-session organization state. The internal reader must return the captured scope exactly, validate organization membership in the same authoritative read as an override, return canonical monotonic decimal revisions, and distinguish an explicit absent override from an unavailable authority. Missing, malformed, regressed, cross-scope, or unavailable authority fails closed without a process cache or last-known-good fallback.
- Managed session assurance separates verified authentication evidence from credential-lifecycle generations. Overrides, generic updates, database hooks, copied sessions, administrators, and external plugins cannot set or inherit assurance fields. Rotation and recovery preserve original authentication timestamps; factor age never refreshes accidentally. Policy changes, organization switches, cached sessions, multi-session selection, one-time-token redemption, device exchange, impersonation, JWT, OIDC, and MCP all revalidate live authority before use or derivation.
- A user who has not yet satisfied managed policy receives a short-lived, single-use, digest-stored, stage-bound pre-session capability for only the allowed passkey or TOTP enrollment/verification steps. No normal session, organization authorization, token derivation, or device approval exists until the final factor proof and a same-revision policy reread succeed. Recovery evidence can enter remediation but cannot satisfy a managed passkey or TOTP requirement for general access.
- New delegated implementation work uses `gpt-5.6-terra` at high reasoning. Terra receives manager-authored bounded contracts and runs targeted tests, lint, and typechecks for its own edits; it does not run builds or own acceptance. The managing Sol agent reviews the exact diff and independently verifies every accepted change.

### Terminal PR and `0.3.0` release gate

- [ ] Freeze all source, schema, package-version, deployment, and release-workflow changes.
- [ ] Confirm every dimension in this ledger is `Proven ahead` with direct evidence.
- [ ] Run targeted verification for each completed unit, then the exact local release rehearsal.
- [ ] Run the complete CI matrix under every supported Node version.
- [ ] Review the full branch diff for correctness, security, generated artifacts, prohibited references, and release consistency.
- [ ] Push the branch once and open a review-ready pull request describing the ledger, implementation, migrations, operational impact, and verification evidence.
- [ ] Resolve every actionable review thread and rerun only the proof invalidated by review changes.
- [ ] Create the immutable `v0.3.0` tag only after the candidate and guarded tag workflow are proven.
- [ ] Publish `0.3.0` through the tag-triggered release path with signed provenance, SBOMs, signatures, and exact tag-commit binding.
- [ ] Verify SHA-512 integrity and signed provenance from the public npm registry, retrying first-publish read-back for up to ten minutes.
- [ ] Verify anonymous installation and anonymous multi-architecture image/chart pulls by immutable digest.
- [ ] Attach final evidence to the release and mark the goal complete only after public visibility is confirmed.

## Comparison baseline

Downloaded Tesseral to [tesseral-reference](/Users/stephenwalker/Code/projects/tesseral-reference) and pinned the comparison to public `main` commit `de5d3348380b820b2670c96728d36e456f696538` from September 10, 2025. Clearance was inspected at `969d6d2901a4a45421cdf27e06caf4ad3b2ed553`, including the current uncommitted worktree.

## Executive verdict

Tesseral is currently the more complete auth product. Its lead comes from customer-facing workflows, a normalized service architecture, durable delivery infrastructure, and productized authentication security.

Clearance is already stronger as an open-source, operator-controlled platform. Its CLI, environment lifecycle, multi-organization identity model, deployment artifacts, recovery tooling, protocol rigor, and release supply chain are materially better.

| Area | Leader | Assessment |
|---|---|---|
| Hosted login and customer portal | Tesseral | Decisive |
| Console and administrative UX | Tesseral | Decisive |
| Database and async architecture | Tesseral | Decisive |
| MFA, passkeys, invitations, branding | Tesseral | Decisive |
| CLI and automation | Clearance | Decisive |
| Self-hosting, recovery, release safety | Clearance | Decisive |
| SAML and SCIM protocol rigor | Clearance | Meaningful |
| Environment and multi-org identity model | Clearance | Meaningful |
| SDK onboarding | Tesseral | Current advantage |
| Runtime/framework implementation breadth | Clearance | Latent advantage |

## Where Tesseral is better

1. **It ships the complete end-user product layer.**

   Tesseral’s Vault covers login, signup, recovery, account security, organization administration, invitations, API keys, audit logs, SAML, OIDC, and SCIM, including guided Google, Entra, and Okta setup flows ([Vault routes](/Users/stephenwalker/Code/projects/tesseral-reference/vault-ui/src/App.tsx:118)). Clearance currently exposes a basic sample application and generic SSO/SCIM setup capability links.

2. **Its operator console models the actual product.**

   Tesseral has nested pages for organizations, users, sessions, passkeys, roles, API keys, authentication policy, enterprise connections, branding, and domains ([console routes](/Users/stephenwalker/Code/projects/tesseral-reference/console/src/App.tsx:91)). Clearance’s console remains a small flat application centered on operational resource lists ([current routes](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-console/public/app.js:13)).

3. **Its core database is designed for scale and querying.**

   Tesseral uses normalized PostgreSQL tables, `sqlc`, and 95 numbered schema migrations. Clearance’s management service still stores global state in one JSONB row, serializes writes with `FOR UPDATE`, rewrites the snapshot, and rebuilds uniqueness tables on mutation ([snapshot store](/Users/stephenwalker/Code/projects/clearance-auth/packages/management/src/store/pg-store.ts:1)). The repository already documents the appropriate normalized replacement in [DESIGN-store-v2.md](/Users/stephenwalker/Code/projects/clearance-auth/DESIGN-store-v2.md:159).

4. **It has a durable delivery plane.**

   Tesseral transactionally enqueues verification, reset, invitation, and webhook jobs, then processes them through a separately deployed River worker. Clearance enables email/password without a configured verification or reset delivery implementation ([product factory](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/create-auth.ts:175)). There is currently no application worker, transactional outbox, webhook delivery, retry, dead-letter, or replay system.

5. **MFA, passkeys, RBAC, and invitations are finished workflows.**

   Tesseral persists WebAuthn credentials, TOTP authenticators, recovery codes, lockout state, invitations, many-to-many role assignments, and organization API-key roles. Clearance contains a latent two-factor plugin, while the product bundle only wires organization, SSO, and SCIM ([plugin construction](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/create-auth.ts:112)). Passkey implementation and invitation operations are absent from the shipped product surface.

6. **Its token and key architecture is more mature.**

   Tesseral stores refresh-token digests, issues five-minute asymmetric access tokens containing resolved actions, and protects rotating per-project signing keys with AWS/GCP KMS abstractions ([access-token issuance](/Users/stephenwalker/Code/projects/tesseral-reference/internal/common/store/accesstoken.go:25)). Clearance has capable JWT machinery in its runtime, though the product does not expose it as the canonical session/access-token architecture.

7. **Its application integration story is clearer.**

   Tesseral documents React, Next.js, Express, Python, Go, and Rust integrations. Clearance has broad framework and datastore code in-tree, while its public product types still include `any` and `unknown` surfaces ([public runtime types](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/public-types/index.ts:51)) and framework onboarding remains thin.

## Clearance’s most serious gaps

- **Managed API keys currently do not authenticate management API calls.** `/v1` still uses the single `CLEARANCE_OPERATOR_TOKEN`. Created key digests and scopes are lifecycle metadata rather than an enforced authorization boundary.

- **Runtime identity activity is missing from the audit ledger.** Management mutations are recorded and redacted well; login, factor, session, SSO, SCIM, and recovery activity needs the same append-only audit sink.

- **Session compromise has a larger blast radius than necessary.** Opaque session values are stored directly. The optional bearer plugin accepts unsigned input by default. Move to token digests, short-lived asymmetric access tokens, refresh rotation, and reuse detection.

- **Custom RBAC is disconnected from application authorization.** Management stores permissions, while runtime membership receives a role slug and sessions lack resolved action claims.

- **Credential purpose separation is incomplete.** Runtime OIDC and SCIM credentials reuse the general auth secret. The existing management keyring is a good foundation for separated key domains and external KMS providers.

## Where Clearance should preserve its lead

- The CLI already covers projects, environments, users, organizations, sessions, roles, SSO, SCIM, diagnostics, migrations, backups, restores, and upgrades ([CLI commands](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-cli/src/index.ts:93)). Tesseral’s CLI is primarily bootstrap and database migration tooling.

- Clearance explicitly models development, preview, and production environments. Tesseral lacks that dimension.

- Clearance separates principals from organization memberships, allowing one identity to belong to several organizations. Tesseral directly associates each user record with one organization.

- Clearance has production Compose, Helm, Terraform, NetworkPolicy, PDB, ServiceMonitor, backup scheduling, restore verification, and rollback guidance. Tesseral’s checked-in deployment stack is development-oriented.

- Clearance’s SAML correlation, replay prevention, destination validation, and default rejection of IdP-initiated login are stronger. Its SCIM implementation also has broader discovery, deprovisioning, and session-revocation behavior.

- Clearance’s signed release, provenance, registry-readback, backup, and recovery discipline is substantially stronger.

## Recommended closure sequence

1. **Normalize management persistence.** Execute store-v2 using expand/backfill, dual writes or equivalent compatibility, checksum verification, cutover, and CLI-visible migration status.

2. **Make credentials real authorization boundaries.** Authenticate managed API keys by digest, enforce project/environment/status/expiry/scopes, attribute audits to key IDs, and retain the operator token only for bootstrap or break-glass use.

3. **Add the durable worker plane.** Implement a transactional outbox, bounded retries, dead-letter state, signed webhooks, SES/SMTP email, and CLI commands for inspection, retry, cancellation, replay, and readiness.

4. **Finish the identity security product.** Productize the existing JWT, TOTP, and breached-password capabilities; add WebAuthn/passkeys, invitation lifecycle, factor policy, recovery, lockout, asymmetric access tokens, KMS providers, and runtime audit events.

5. **Ship `@clearance/vault`.** Cover hosted login/signup/recovery, organization switching, account security, invitations, customer-managed enterprise connections, API keys, and audit logs.

6. **Rebuild the console around typed nested resources.** Use the canonical operation registry to generate runtime schemas and browser/server clients. Preserve exact CLI equivalents for every mutation.

7. **Complete the ecosystem.** Add React/Next/Express guides first, then thin Python/Go/Rust verification libraries, OpenTelemetry tracing, branding/domains, and multi-architecture images.

The strongest direction is to retain Clearance’s CLI and operations model while adopting Tesseral’s product completeness, normalized persistence, worker architecture, and security workflow coverage.
