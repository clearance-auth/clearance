# Tesseral parity and lead plan

## Live execution ledger

- **Status:** In progress
- **Progress:** 31/37 (84%)
- **Branch:** `tesseral-parity-and-lead`
- **Canonical ledger:** `/Users/stephenwalker/Code/projects/clearance-auth/plans/2026-07-15-tesseral-comparison-gap-closure-plan.md`
- **Active goal:** `019f636b-e317-7b42-a47d-0f9f8db7de44`
- **Release target:** `0.3.0`
- **Goal:** Make Clearance demonstrably better than Tesseral on every dimension identified in this comparison while preserving Clearance's existing leads.
- **Completion rule:** Every row below must be `Proven ahead`, with direct source, CLI, live-product, operational, or release evidence recorded in this file. A feature without an API and CLI workflow is incomplete. Completion includes a review-ready pull request followed by the signed `0.3.0` release and public-registry verification.
- **Ledger rule:** Update status, evidence, decisions, dependencies, and next actions after each completed implementation unit. Current code and observed behavior remain the source of truth.

### Engineering-first execution policy

This policy governs every remaining implementation round so progress concentrates on product and engineering outcomes instead of process volume.

- Start each round by identifying the highest-leverage dependency that makes a plan requirement materially more true. Prefer central authority, storage, protocol, API, or workflow primitives that close several call sites over route-specific patches.
- Before assigning work, identify one or more optimization paths that reduce duplicated implementation, repeated context loading, redundant verification, serial dependencies, or review churn. Adopt the safest useful optimization immediately and record material changes in the execution log.
- Keep one cohesive implementation lane per dependent unit. Run truly disjoint units concurrently after their shared contract is stable; never create parallel lanes that edit the same files or invent competing architecture.
- Give delegated workers bounded engineering contracts, exact invariants, file ownership, and targeted verification. The manager owns architecture, integration, source inspection, commits, and final acceptance.
- Add tests only for an explicit acceptance invariant, a reproduced failure, or a regression that existing proof cannot detect. Prefer one strong deterministic case over several overlapping mock, adapter, and endpoint variants.
- During implementation, run only the narrow suite and typecheck needed to shorten the feedback loop. At unit completion, run one integrated affected-surface gate, one typecheck, and diff hygiene. Do not rerun unchanged proof or broad builds without an invalidating source, schema, release-path, runtime, or dependency change.
- Use one adversarial review for the cohesive integrated unit. Batch its actionable findings into one repair pass, then rereview only the changed authority boundary. Avoid reviewer fan-out that covers the same invariant repeatedly.
- Update this ledger once per meaningful completed unit with source evidence, the minimal sufficient verification, dependencies cleared, remaining risk, and the next highest-leverage action. Test counts and review volume are evidence, never progress units by themselves.
- At the end of each round, perform a brief optimization check: what consumed time without changing the product, what can be centralized or deleted, what work can safely run in parallel next, and which proof can be reused because its authority was not invalidated.
- Reserve package builds, broad CI matrices, release rehearsals, deployment proof, and publication checks for the gates that actually require them, especially the frozen `0.3.0` candidate.

### Status legend

- `In progress`: active implementation work.
- `Queued`: accepted work with unmet dependencies.
- `Lead to protect`: Clearance currently leads; regression proof remains required.
- `Proven ahead`: implemented and verified against the stated acceptance bar.

### Dimension ledger

| Dimension | Acceptance bar for Clearance to lead | Status | Evidence |
|---|---|---|---|
| Normalized persistence and query scale | Normalized relational management data, row-level concurrency, online verified migration, stable cursor queries, and measured 5k/50k performance ahead of the snapshot baseline | In progress | Audit events and principals have atomic reversible relational authority and passing 5k/50k production-path gates. Commits `1c1cb84` through `f110b4f` add the project/environment/organization authority kernel: exact parent-chain SQL constraints, millisecond-safe stable cursors, typed bounded repositories, per-row concurrency without snapshot locking or full-table mutation counts, empty authoritative snapshot arrays, generic-writer fencing, and exact rollback materialization. Commits `feea2a1` through `b3723c4` expose confirmed API/CLI cutover and rollback, while `d1cf4d4` through `dc63b96` add transaction-owned topology-plus-audit writes, exact cached counts, cross-process refresh, and one monotonic local publication authority for snapshot, topology, principal, and event projections. Commits `3e5b33d` through `cc36f08` move scoped project, environment, organization, overview, and API reads onto bounded relational point/count/keyset queries with explicit unavailability instead of false snapshot-derived counts. Lifecycle writes, auxiliary consumers, and the final 5k/50k production-path comparison remain |
| Credential authentication and authorization | Managed keys authenticate by digest; status, expiry, project, environment, and route scopes are enforced; key identity appears in audit; operator token is bootstrap/break-glass only | Proven ahead | Digest-only authentication, expiry/revocation, key-derived scope, route read/write scopes, operator-only topology/recovery/configuration/tests, audit attribution, CLI expiry, and API-key-aware CLI `whoami`; live CLI proof created an expiring key, authenticated `whoami`, listed users, and rejected `users:write` with exit 1 |
| Durable jobs, email, and webhooks | Transactional outbox, separately deployable worker, retries, dead letters, signed delivery, replay, SES/SMTP, quotas, readiness, and complete CLI control | In progress | Commit `762fc55` makes signup, explicit verification, password reset, invitation create/resend, sign-in/username verification resend, social signup verification, and email-change verification use the durable path; product mutations plus encrypted outbox rows share the ambient Postgres transaction. Commits `d5231d3` and `6a96af8` add schema-v3 fingerprint-key identity, retained-key readiness, rotation-stable generation dedupe, explicit legacy migration, and queued-job processing across rotation. Commit `276f6ec` adds the management PgStore outbox seam and canonical encrypted `organization.updated` delivery intents in the same runtime/management transaction. Commit `2e8ee86` adds exact-byte event-bound HMAC webhook delivery with stable idempotency, HTTPS-by-default endpoints, all-answer global-unicast DNS validation, pinned connections, redirect refusal, bounded DNS/connect/response work, destination-fingerprint verification, and redacted durable outcomes. Commits `b21d89b`, `89c2066`, and `b944ce8` add project/environment-scoped redacted inspection, quotas, operator-only readiness with live managed-key denial proof, transaction-locked retry/cancel/replay previews, leased-cancellation intent, audited mutations, stable API/CLI job-control contracts, strict producer configuration, additive scoped-query indexing, and encrypted managed endpoint storage/execution. Commit `3368890` completes preview-first audited endpoint create/update/rotate/delete/test, active-subscription fanout, canonical URL deduplication, pending/failed/succeeded readiness, one-time secrets, exact API/CLI lifecycle parity, and operator/managed-key authorization. Commit `2cd19f5` adds explicit AWS SES v2 delivery with fixed regional endpoints, SigV4, bounded HTTPS work, non-sending account readiness, stable hashed delivery identity, sanitized provider outcomes, and SMTP-compatible transport-neutral worker health. Commits `c95809f` and `5e942bb` deploy and harden the worker in production Compose and Helm with one API/worker database authority, immutable image references with release-gated signature proof, fail-closed shared key and transport configuration, retained-key validation, bounded graceful drain, concrete resources, least-privilege pod security, destination-scoped DNS/database/SMTP egress, internal readiness/metrics, PDB, and NetworkPolicy-gated ServiceMonitor. Live SES-account proof remains |
| Authentication security | Supported passkeys, TOTP, recovery, factor policy, breached-password defense, account lockout, digest-stored refresh/session secrets, rotating asymmetric access tokens, purpose-separated keys, and KMS providers | Proven ahead | Commits `f871112`, `158b5da`, `abf3758`, `708647f`, `9f8a22e`, and `5eb30a3` establish bounded password defense, encrypted OAuth and recovery material, replay-safe factor enrollment, one-way recovery codes, trusted-device/session generation, standards-complete passkeys, last-viable-factor protection, and one credential-account lockout authority across email, username, and phone. Item 21 adds digest-only session and OAuth authority with atomic refresh rotation/reuse revocation, five-minute Ed25519 access tokens, and a durable generation fence. Commits `6f3dea0` through `617309a` add normalized revision-CAS environment/organization policy, exact unlock, atomic audit, and preview-first API/CLI control. Units 5A-5D (`e6e6aaf` through `fc6427d`) add purpose-bound local/AWS/GCP KMS encryption and signing, exact resource binding, atomic legacy migration, retired-private-key destruction, retained public JWKS, redacted readiness, deterministic migration plans, bounded transaction-proven apply, atomic audit, and exact API/CLI preview workflows. Unit 6 (`40c441b` through `8a904be`) completes transaction-owned runtime security audit across every authentication authority. |
| Runtime auditability | Append-only login, recovery, factor, session, SSO, SCIM, API-key, invitation, webhook, and impersonation events with actor, target, outcome, network context, and correlation | Proven ahead | Commits `40c441b` through `8a904be` add one transaction-owned append-only runtime authority for login, session, refresh, recovery, factor, impersonation, invitation, SSO, SCIM, webhook queue, retry, delivery, and dead-letter facts. API-key events remain on their existing atomic management authority. The existing scoped list/tail/inspect/export API and CLI merge both normalized authorities with stable cursor ordering, bounded reads, no cache, exact project/environment/organization/action scope, and centralized credential/email/URL redaction. Real PostgreSQL proof covers commit/rollback, append-only guards, schema drift, delivery transitions, and operational reader behavior. |
| RBAC and tenant credentials | Canonical many-to-many role/action assignments for people and organization service accounts, consistent runtime action claims, immediate revocation/version behavior, API/CLI/UI parity | In progress | Units 7A-7C are complete: one verified seven-table PostgreSQL authority owns canonical actions, immutable built-ins, custom roles, many-to-many subject assignments, service accounts, digest-only credentials, and monotonic organization revisions. Management membership and role mutations use that authority in their existing transaction; bounded one-organization reconciliation clears stale assignments; new management and product organizations atomically receive owner authority. Session and machine JWTs carry sorted actions plus revision and reject stale, generic, partial, mixed, authority-free, or custom-signer-mutated claims. Service-account credentials have exact scope, one-time secret delivery, uniform invalid-credential behavior, wall-clock expiry, rotation/revocation/disable invalidation, and credential-expiry-capped tokens. API/CLI/operator-console/Vault workflows remain. |
| Hosted login and customer portal | A production-quality `@clearance/vault` covers login, signup, recovery, organization switching, account security, invitations, enterprise connections, API keys, policy, and audit logs | Queued | Starts after foundational identity workflows stabilize |
| Operator console | Typed nested resource UI with accessible components, generated client, complete operational and product workflows, and exact CLI equivalents | Queued | Preserve current security-focused BFF posture |
| Branding, domains, and enterprise setup | Theme/branding, custom-domain verification, email-domain readiness, templates, provider-guided SAML/OIDC/SCIM setup, testing, and actionable failure ownership | Queued | Builds on Vault, worker, and readiness infrastructure |
| Typed API and SDK onboarding | Runtime-validated canonical contract, generated browser/server clients, precise public types, runnable React/Next/Express paths, then Python/Go/Rust verification SDKs with shared conformance fixtures | Queued | Existing in-tree framework/runtime breadth is the implementation base |
| Observability and image portability | OpenTelemetry across HTTP/Postgres/external protocols/jobs, existing Prometheus retained, multi-architecture application/worker/backup images, anonymous digest-pull proof | In progress | Commit `1c7a048` adds bounded low-cardinality worker claim/outcome/duration/readiness gauges and a Prometheus `/metrics` surface with no tenant, job, event, endpoint, or error labels. Commit `c95809f` exposes that internal surface through a selector-scoped ServiceMonitor and dedicated NetworkPolicy while keeping the worker free of public ingress; OpenTelemetry, multi-architecture, and anonymous digest-pull proof remain |
| CLI and automation | Every workflow is scriptable with stable JSON, noninteractive operation, dry-run/plan where meaningful, structured remediation, and complete API parity | Lead to protect | Existing CLI breadth exceeds Tesseral; add regression/parity proof per new workflow |
| Environment and identity model | Development/preview/production lifecycle and multi-organization principals remain first-class across every new feature | Lead to protect | Existing Clearance model is ahead; every schema and workflow must preserve it |
| SAML and SCIM rigor | Preserve request correlation, replay defense, binding validation, scoped deprovisioning, discovery, session revocation, and fail-closed behavior; close SCIM Groups | Lead to protect | Existing implementation is stronger; SCIM Groups remains queued work |
| Self-hosting, recovery, and release safety | Production Compose/Helm/Terraform, backup/restore/rollback, signed provenance, registry read-back, complete Node matrix, and worker/Vault deployment remain one verified release system | Lead to protect | Existing operations and release gates are ahead; extend them for every new component |

### Active work

- [x] Define and land the storage core for the first reversible store-v2 schema/backfill/verification slice.
- [x] Expose store-v2 plan/status/apply/verify/rollback through the operation registry, API, CLI, and doctor.
- [x] Make managed API keys real `/v1` authenticators with route-scope enforcement and attribution.
- [x] Define the outbox transaction seam against store-v2 so job delivery never races product state.
- [x] Record targeted verification and update dimension statuses for the first mergeable foundation unit.
- [x] Land the first reversible relational-authority slice for audit events.
- [x] Expose audit-event cutover and rollback through API, CLI, and doctor with explicit confirmation.
- [x] Prove the authoritative audit append path at 5k and 50k retained events against the ledger thresholds.
- [x] Cut over principals to normalized relational authority with typed repository writes, reversible migration, cursor reads, snapshot fencing, and 5k/50k production-path proof.
- [x] Close strict-review blockers in the delivery storage and worker core before accepting it.
- [x] Wire signup, verification, reset, and invitation state plus delivery enqueue into one runtime transaction.
- [x] Ship the separately deployable TLS-default SMTP worker with retries, dead letters, lease renewal, provider-acceptance checkpointing, readiness, and graceful drain.
- [x] Version delivery fingerprint keys and prove rotation without losing queued jobs or weakening generation dedupe.
- [x] Add exact-byte signed webhook delivery with event-bound HMAC, idempotency, DNS pinning, redirect refusal, bounded responses, and explicit loopback-only development HTTP.
- [x] Ship scoped delivery inspection, readiness, quotas, retry/cancel/replay controls, and exact CLI/API parity.
- [x] Ship encrypted customer-managed webhook endpoint resources, event subscriptions, secret rotation, test delivery, and exact API/CLI lifecycle control.
- [x] Add SES transport with readiness and failure classification.
- [x] Add delivery observability and deployment manifests.
- [x] Productize bounded breached-password defense, encrypted OAuth credentials, staged replay-safe TOTP, concurrent account lockout, rotating Ed25519 access tokens, legacy-key migration, and precise public APIs.
- [x] Harden recovery and factor lifecycle with one-way recovery codes, step-up disable/regeneration, trusted-device revocation, and session revocation.
- [x] Store session and refresh secrets by digest with rotation, reuse detection, family revocation, and short-lived asymmetric access-token integration.
- [x] Add standards-complete WebAuthn/passkey registration, authentication, discovery, revocation, and recovery-safe policy behavior.
- [x] Add managed factor policy and password/account lockout with runtime enforcement and API/CLI control.
  - [x] Unit 1: credential-row password-attempt authority shared across email, username, and phone.
  - [x] Unit 2: durable scoped policy, session assurance, and restricted enrollment with no session-issuance bypass.
    - [x] Unit 2A: immutable runtime scope, uncached reader contract, canonical policy validation, and reserved session authority fields.
    - [x] Unit 2B: explicit server-derived issuance evidence plus live validation for create, read, rotation, recovery, update, cache, and publication paths.
    - [x] Unit 2C: digest-only staged enrollment/verification capability with passkey and TOTP completion and no general session before policy satisfaction. Recovery remains a separate digest-only, session-ineligible repair authority: a one-shot recovery proof may repair a lost passkey or TOTP, but can never become session evidence; access still requires a fresh normal primary plus proof of the repaired factor.
    - [x] Unit 2D: exact organization binding plus multi-session, one-time-token, device, impersonation, JWT, OIDC, and MCP derivative-credential enforcement.
  - [x] Unit 3: product PostgreSQL policy authority and verified migration.
  - [x] Unit 4: revisioned get/plan/apply/unlock API and CLI workflows.
- [x] Separate credential purposes and add AWS/GCP KMS-backed signing/encryption providers with local development parity.
  - [x] Unit 5A: ship one provider-neutral key-management package with purpose-bound local, AWS KMS, and GCP Cloud KMS encryption, rotation-aware envelopes, retained-key readiness, and dependency-injected cloud clients.
  - [x] Unit 5B: move OIDC client secrets, SCIM bearer-token bases, and stored access-token signing keys off the general auth secret with fail-closed legacy read and online re-encryption.
  - [x] Unit 5C: add AWS/GCP asymmetric remote signing with public-JWK publication and local software-signing parity, preserving source-session authority, five-minute expiry, rotation grace, and transaction behavior.
  - [x] Unit 5D: expose redacted status, readiness, migration plan/apply, and rotation evidence through atomic management operations plus exact API/CLI workflows.
- [x] Add append-only runtime authentication audit events and exact API/CLI inspection and mutation workflows for every security feature.
  - [x] Unit 6A: add the transaction-owned append-only runtime audit outbox, canonical event contract, request correlation/network context, and session/login success and failure events.
  - [x] Unit 6B: add recovery, factor, refresh rotation/recovery/reuse, and impersonation events at their existing transaction authorities.
  - [x] Unit 6C: add SSO, SCIM, invitation, and asynchronous webhook lifecycle events without duplicating existing API-key management audit.
  - [x] Unit 6D: merge runtime events into the existing scoped list/tail/inspect/export readers and prove exact API/CLI visibility, ordering, redaction, rollback, and append-only behavior.
- [ ] Normalize RBAC and organization service-account credentials behind one runtime authorization authority.
  - [x] Unit 7A: add scoped Postgres action, role, role-action, subject-assignment, service-account, credential, and organization-revision authority with verified migration and uncached effective-action reads.
  - [x] Unit 7B: reconcile existing membership/custom-role state into many-to-many person assignments and make management role/action/assignment operations use the normalized authority.
  - [x] Unit 7C: add digest-only organization service-account credentials, immediate disable/revoke/rotation behavior, runtime machine-principal authentication, and revision-bound sorted action claims.
  - [ ] Unit 7D: expose exact dry-run/confirmation API and CLI workflows, register every surface, then add console administration and Vault tenant self-service at their authenticated UI boundaries.
    - [x] Unit 7D1: add canonical effective-authorization, assignment, bounded-reconciliation, service-account, and credential operation contracts plus thin scoped management authority adapters.
    - [x] Unit 7D2: expose those frozen operations through exact API paths and noninteractive JSON CLI commands, with one-time-secret replay redaction and confirmation on replacement, disable, rotation, revocation, and reconciliation.
    - [x] Unit 7D3: register existing role workflows and every new authorization workflow, then add the two operator-console routes without changing the console's operator-only trust boundary.
    - [ ] Unit 7D4: expose tenant-safe role-assignment and service-account administration through the future authenticated Vault boundary; the machine credential exchange remains a runtime-only server API and never crosses the operator management plane.
- [ ] Complete normalized project, environment, and organization authority without retaining a snapshot-shaped production read path.
  - [x] Unit T1: land the reversible parent-chain authority kernel with bounded reads, typed writes, exact count/revision state, snapshot fencing, nonblocking writers, stable cursors, and rollback materialization.
  - [ ] Unit T2: add one transaction-owned topology-plus-audit mutation seam and migrate core project/environment/organization reads and writes to it; preserve the JSON backend through explicit synchronous fallbacks.
    - [x] Unit T2A: make changed topology writes require atomic append-only audit, publish exact count/state without snapshot scans, serialize same-process cache publication without serializing database writes, and expose confirmed cutover/rollback API and CLI workflows.
    - [x] Unit T2B: migrate scoped project, environment, organization, and overview reads to bounded relational readers while retaining synchronous JSON fallbacks.
    - [ ] Unit T2C: migrate core project/environment/organization lifecycle writes to the audited topology transaction without restoring snapshot topology.
  - [ ] Unit T3: migrate remaining scope, configuration, doctor, identity, migration, membership, authentication-policy, enterprise, and API bootstrap consumers; then run one integrated 5k/50k production-path comparison.
- [ ] Derive the shared runtime-validated browser/server client from the canonical operation registry before building more UI transport by hand.
- [ ] Build one authenticated product shell that supplies Vault and nested-console workflows, then layer branding, domains, templates, and guided enterprise setup on the same primitives.
- [ ] Complete SCIM Groups and correlated OpenTelemetry propagation in disjoint lanes once the topology integration files are stable.

### Execution log

| Date | Change | Evidence / next action |
|---|---|---|
| 2026-07-15 | Created branch `tesseral-parity-and-lead` and activated the plan-linked goal | Goal `019f636b-e317-7b42-a47d-0f9f8db7de44`; begin credential enforcement and store-v2 foundation |
| 2026-07-15 | Started managed API-key implementation lane | Inspect live authentication middleware and add narrowly scoped tests; manager verification required |
| 2026-07-15 | Started read-only store-v2 implementation-slice review | Select a reversible first slice that preserves the existing JSON backend and avoids speculative rewrites |
| 2026-07-15 | Set terminal release target to `0.3.0` | Open a review-ready PR after implementation and complete proof; release only after approval and the full release gate |
| 2026-07-15 | Accepted store-v2 Slice 1 boundary | Normalize projects, environments, principals, and organizations behind explicit apply; snapshot stays authoritative during verified shadow mode; storage implementation started |
| 2026-07-15 | Accepted delivery-plane dependency boundary | Runtime email outbox may precede store-v2; management webhook emission waits for transactional delivery intents; encrypted payloads and signed HTTPS delivery are mandatory |
| 2026-07-15 | Implemented managed API-key authentication | Full API suite 88/88, focused auth/security 5/5, management key lifecycle 6/6, CLI auth and transport 18/18; API, management, and CLI typechecks green |
| 2026-07-15 | Proved managed-key operation through the live API and CLI | CLI created an expiring scoped key, `whoami` identified the key and exact scopes, `users list` succeeded, and `users create` failed with `API_KEY_SCOPE_FORBIDDEN` and exit 1; live proof artifacts moved to Trash |
| 2026-07-15 | Landed store-v2 Slice 1 storage core | Pure helpers 4/4; canonical real-Postgres regression 34/34 across shadow, revision drift, atomic swaps, environment reparenting, concurrency, user lifecycle, and organization lifecycle; management typecheck and diff check green |
| 2026-07-15 | Hardened store-v2 adoption boundaries during manager review | Apply refuses unowned target-table collisions, invalid phase metadata, and future schema versions without touching external data |
| 2026-07-15 | Completed store-v2 control-plane integration | Stable registry/API/CLI envelopes for status, plan, apply, verify, and rollback; two-sided confirmation, doctor visibility, structured failure, operation contracts 9/9, API operations 5/5, CLI transport 11/11 |
| 2026-07-15 | Closed two strict security reviews on the foundation | Fixed missed-writer revision masking, valid unique swaps and environment reparenting, forged enterprise audit actors, raw settings exposure, expired rotation and dry-run, case-variant staging, and SCIM SSRF using all-answer validation, global-unicast policy, pinned DNS, redirect refusal, and operator-only enterprise probes; final review reported no blocker |
| 2026-07-15 | Committed the first mergeable foundation unit | Commit `ca361e7` contains managed API authorization, store-v2 shadow/control plane, strict-review hardening, live-ledger evidence, and no live secret material |
| 2026-07-15 | Selected first relational-authority cutover | Audit events move first into a hybrid authoritative store: immutable append cost becomes O(1), the JSON backend remains unchanged, rollback reverse-materializes the visible projection, and 5k/50k production-path append benchmarks gate acceptance; storage implementation started |
| 2026-07-15 | Froze the delivery-plane transaction and security boundary | Runtime email only in Slice 1; product state plus immutable event, encrypted payload, and initial job must use the ambient runtime adapter transaction; no pool, second Kysely instance, background helper, or post-commit hook; management webhooks remain deferred |
| 2026-07-15 | Started delivery storage/worker-core implementation | New `@clearance/delivery` package is limited to guarded schema migration, purpose-specific AEAD/HMAC keyring, transaction-adapter enqueue, fenced leasing/retry/dead/cancel/reclaim, retention crypto-erasure, redacted views, and Postgres proof; SMTP/runtime/API/CLI/deploy wiring follows after core review |
| 2026-07-15 | Completed the audit-event relational-authority core (`1431e88`) | Hybrid cutover empties the snapshot event projection, relational appends and resource mutations share one transaction, cursor reads are stable, retention hides historical rows instead of deleting them, and rollback reverse-materializes the visible projection; management typecheck, focused unit 7/7, and canonical real-Postgres 35/35 passed |
| 2026-07-15 | Rejected the initial delivery core at strict review | Active transactions, tenant-scoped dedupe, semantic-expiry completion, canonical verification inclusion, lease-owner binding, safe erasure, channel-bound AAD, schema-drift detection, structural retention, and provider-value redaction must close before commit |
| 2026-07-15 | Completed audit-event authority control-plane integration (`10646f6`) | Added confirmed API and CLI cutover/rollback operations, operation-registry coverage, and hybrid-authority doctor evidence; management contracts 9/9, API operations 5/5, CLI transport 11/11, and all three package typechecks passed |
| 2026-07-15 | Accepted hardened delivery storage and worker-facing primitives (`5c8b2a9`) | Strict-review blockers closed: active transaction savepoint enforcement, tenant-scoped dedupe, channel-bound AEAD, owner-fenced leases, semantic-expiry dead-lettering, atomic erasure, bounded retention, schema-shape verification, opaque provider metadata, and fail-closed canonical Postgres/CI inclusion; delivery build and five-package typecheck passed; canonical Postgres gate passed management 250/250 and delivery 9/9 with zero skips |
| 2026-07-15 | Proved authoritative audit scale on the production PgStore path (`9ff5667`) | Manager rerun with five warmups and 50 samples: 5k p50/p95 6.35/6.75ms; 50k p50/p95 8.29/9.78ms; 50k flatness gate 9.78 <= 18.50ms; snapshot event projection remained 2 bytes with zero growth; normalized persistence remains in progress pending remaining collection cutovers |
| 2026-07-15 | Completed atomic runtime email enqueue (`762fc55`) | Signup hashes and creates its verification generation before opening the real durable transaction; user, credential account, session, and outbox mutations then commit together; reset token, invitation create/resend, email changes, and unverified social signup share their enqueue transaction; explicit and sign-in/username verification resend use the same durable contract; legacy configurations retain callback behavior; four real-Postgres proofs cover five committed jobs, all-five worker processing, enqueue-failure rollback, and later-mutation/hook rollback |
| 2026-07-15 | Completed the first deployable SMTP worker (`762fc55`) | TLS or STARTTLS is mandatory except explicit unauthenticated loopback development; canonical verification/reset/invitation rendering is bounded and HTML-escaped; destination fingerprints are checked before send; leases renew during slow providers; accepted outcomes are durably checkpointed and ambiguous accepted jobs dead-letter instead of intentional resend; readiness rechecks DB, owned schema v2, required tables, SMTP, keyring, and heartbeat; drain timeout force-closes resources and returns failure |
| 2026-07-15 | Proved the integrated delivery slice through the canonical Postgres gate | Manager rerun: management 250/250, delivery 12/12, delivery-worker 14/14, and clearance-auth 22/22, all with machine-asserted zero skips; six affected packages typecheck and the three publishable packages build; focused affected-runtime verification passed 199/200 with one pre-existing declared skip, wrapper 8/8, and core transaction 4/4; `git diff --check` passed |
| 2026-07-15 | Recorded strict-review follow-up for key rotation and control plane | Before the delivery dimension can be `Proven ahead`, fingerprint keys must be versioned and retained across queued jobs, and scoped/redacted API plus CLI controls must land atomically with immutable operator audit and quota enforcement |
| 2026-07-15 | Completed delivery fingerprint-key rotation (`d5231d3`, `6a96af8`) | Schema v3 records source and destination fingerprint key IDs, keeps a purpose-separated stable generation-dedupe authority, transactionally upgrades owned v1/v2 data only with an explicit retained legacy key ID, refuses premature key retirement, processes queued jobs created under retained old keys, and rechecks key availability on every worker readiness probe |
| 2026-07-15 | Completed the management transactional delivery-intent seam (`276f6ec`) | PgStore exposes an opaque enqueue closure bound to its active transaction; canonical `organization.updated` intents carry project/environment/organization/actor/correlation attribution while URL and signing secret remain encrypted; real Postgres proof covers commit, enqueue-failure rollback, later-mutation rollback, concurrent dedupe, environment isolation, and fail-closed unconfigured stores |
| 2026-07-15 | Made the canonical PostgreSQL gate concurrency-safe (`2f1f8eb`) | Per-process container names prevent parallel verification runs from deleting each other's databases; the accepted rerun passed management 257/257, delivery 13/13, delivery-worker 15/15, and clearance-auth 22/22 with machine-asserted zero skips |
| 2026-07-15 | Accepted the next delivery dependency boundary | Build signed exact-byte HTTPS webhook transport and endpoint hardening first, then scoped/redacted API and CLI inspection/retry/cancel/replay/readiness, quotas, SES, observability, and deployment manifests |
| 2026-07-15 | Completed signed webhook transport (`2e8ee86`) | Canonical event bytes are signed and sent unchanged; signatures bind event ID and timestamp, stable job IDs provide idempotency, endpoint credentials stay inside the encrypted payload, all DNS answers must satisfy the global-unicast policy, the selected address is pinned while preserving TLS hostname validation, redirects are terminal, and DNS/connect/response bytes/time are bounded. The invalidated first gate exposed and fixed an oversized-response settlement race; the accepted rerun passed management 257/257, delivery 13/13, delivery-worker 24/24, and clearance-auth 22/22 with zero skips. Next: scoped/redacted controls and quotas |
| 2026-07-15 | Completed scoped delivery operations (`b21d89b`) | Added redacted project/environment list and inspect, scoped quota status, operator-only global readiness, preview-first audited retry/cancel/replay, strict JSON and CLI option parsing, exact operation-registry/API/CLI parity, validated producer key/quota configuration, and a repairable scoped event index. API passed 94/94, focused CLI 21/21, four package typechecks passed, and the accepted canonical Postgres rerun passed management 267/267, delivery 18/18, delivery-worker 24/24, and clearance-auth 22/22 with machine-asserted zero skips. Next: customer-managed endpoint resources and SES transport |
| 2026-07-15 | Hardened delivery route authorization proof (`89c2066`) | Added live managed-key denial proof for operator-only global readiness and removed a redundant post-commit flush because coordinated delivery mutations already resolve after commit; focused API 11/11 and API typecheck passed |
| 2026-07-15 | Closed replay-preview contract mismatch (`adcba44`) | Replay previews now validate and display the requested `maxAttempts` override that confirmed execution will use; delivery and management typechecks, focused management 3/3, API 6/6, and live-Postgres control 5/5 passed |
| 2026-07-15 | Selected principal relational authority as store-v2 Slice 2 | Principals are the smallest dependency-correct high-cardinality cutover that removes the measured principal-create snapshot rewrite and rebuilt email index. The slice requires typed transaction-bound reads/writes, `principals: []` snapshot fencing, authority revision refresh, explicit cutover/rollback controls, real cursor reads, and 5k/50k gates of p50 <25ms, p95 <50ms, and 50k p95 <= 2x 5k p95 + 5ms; events remain relational-authoritative while projects, environments, and organizations remain shadowed |
| 2026-07-15 | Completed AWS SES transport (`2cd19f5`) | Explicit `ses` selection uses the SES v2 HTTPS API at a validated fixed regional host, deterministic SigV4, static or session credentials from Clearance/AWS environment names, bounded timeout and 64 KiB responses, redirect refusal, non-sending `GetAccount` readiness, stable hashed delivery headers, hashed provider IDs, and sanitized retry/permanent classifications. SMTP remains the default and passed its real compatibility gate. Worker build and four affected typechecks passed; the accepted canonical gate passed management 267/267, delivery 18/18, delivery-worker 28/28, and clearance-auth 22/22 with zero skips. Live SES-account proof remains required in a credentialed release environment |
| 2026-07-15 | Froze customer-managed webhook endpoint contract | Delivery schema v4 will add normalized project/environment endpoint resources with purpose-bound encrypted URL/secret configuration, one-time generated secret output, exact closed event subscriptions, optimistic versions, crypto-erasing soft delete, endpoint provenance on immutable events, preview-first rotate/delete/test, atomic audit/enqueue, and exact scoped API/CLI parity. Runtime DNS and pinned HTTPS validation remains mandatory on every attempt; create-time validation never becomes durable network trust |
| 2026-07-15 | Added delivery-worker Prometheus foundation (`1c7a048`) | Worker claims, bounded outcomes, cumulative processing duration, in-flight work, drain state, schema health, email-transport health, and uptime are exposed at `/metrics` without high-cardinality or sensitive labels. Focused unit tests 11/11, worker typecheck/build, and the schema-v4 real-Postgres worker rerun 22/22 passed; Compose/Helm deployment and end-to-end telemetry remain under review |
| 2026-07-15 | Deployed the transactional worker in production Compose and Helm (`c95809f`) | API and worker share fail-closed keyring, quota, schema, and transport configuration; production uses immutable digest images, a 310-second Compose stop grace, Helm grace/drain validation, hardened pod security, PDB, internal health/metrics Service, selector-scoped ServiceMonitor, and dedicated DNS/database/HTTPS plus destination-scoped SMTP egress. Manager verification passed Helm lint/default, SMTP, SES, metrics, maximum integer rendering, unsafe egress/path rejection, production environment validation, resolved Compose hardening, and `git diff --check`. Next: endpoint lifecycle and full delivery integration gate |
| 2026-07-15 | Closed strict deployment review (`5e942bb`) | Worker database configuration now comes from the API Secret authority; IPv4/IPv6 allow-all SMTP CIDRs, unscoped DNS/database egress, insecure SMTP, ServiceMonitor without NetworkPolicy, unknown retained fingerprint IDs, and empty resources fail closed. Digest syntax is described accurately while signature/provenance remains a terminal release proof. Independent focused operations verification passed every static, environment, Compose, default/SMTP/SES Helm, and adversarial render check through the pre-Postgres section with zero failures; full backup/restore proof remains part of the release gate. |
| 2026-07-15 | Accepted managed endpoint storage and worker execution (`b944ce8`) | Schema v4 adds project/environment-scoped encrypted endpoint config, retained-key readiness, strict constraints/index drift verification, optimistic versions, active-subscription fanout, destination provenance, one-time generated secrets, canonical URL views, and crypto-erasing delete with queued cancellation plus leased cancellation requests. Exact `webhook.endpoint.test` payloads are transactionally enqueued, event-bound HMAC signed, and delivered through the pinned HTTPS path; managed enqueue/fanout/test/replay honor configured quotas and port 443 matches production egress. Manager rerun passed delivery 28/28, delivery-worker 31/31, both typechecks, and diff checks against live PostgreSQL. Next: preview-first audited management lifecycle with exact API/CLI parity. |
| 2026-07-15 | Completed principal relational authority (`3368890`) | Typed project/environment-scoped writes and relational reads now cover user lifecycle, sessions, members, export, migration, SCIM, and runtime identity sync. A transactionally locked `{revision,count}` state publishes only committed counts, staggered cross-store writers refresh to the authoritative SQL count, public callbacks receive a frozen allowlisted repository with no raw SQL, rollback physical deletion requires an internal exact checkpoint, failed mutations leave revision unchanged, and rollback/re-import succeeds. Principal PG and 5k/50k scale tests passed 2/2; the strict reviewer closed cache coherence and rollback-capability exposure. Remaining persistence work: projects, environments, and organizations. |
| 2026-07-15 | Completed managed webhook endpoint lifecycle (`3368890`) | Preview-first audited create/update/rotate/delete/test, one-time secret output, exact subscriptions, optimistic versions, active fanout, canonical URL dedupe across managed and legacy targets, crypto-erasing deletion, and pending/failed/succeeded test readiness now have management/API/CLI parity. Strict review closed queued-test false readiness and double-delivery risk. Delivery passed 28/28 on required PostgreSQL; API passed 100/100; CLI passed 94/94. Live SES-account proof is the remaining delivery-dimension gate. |
| 2026-07-15 | Accepted integrated principal and endpoint proof | Required-Postgres management passed 287/287; focused principal/delivery/concurrency/rollback proofs passed 35/35; management built; management, API, CLI, and delivery typechecks passed; internal rollback/SQL capabilities were absent from public build exports; `git diff --check` passed. Next: authentication-security foundation in dependency order. |
| 2026-07-15 | Completed the authentication-security foundation (`f871112`) | Product defaults now wire bounded HIBP checks, encrypted OAuth tokens, idempotent encrypted TOTP enrollment, old-factor-proven staged replacement, atomic timestep replay defense, a hard concurrent account-attempt ceiling, rotating five-minute Ed25519 tokens, public-only JWKS, bounded immutable legacy verification grace, and conditional typed server/client APIs. Runtime passed 157/157; product passed 14/14; required Postgres passed 4/4 including repeated-migration retirement, one-row factor authority, concurrent rotation, encrypted private keys, and legacy schema upgrade; runtime/auth typechecks and utils/runtime/auth builds passed; final strict review found no P0-P2. Next: one-way recovery and step-up factor lifecycle. |
| 2026-07-15 | Completed recovery and factor lifecycle hardening (`158b5da`) | Recovery codes are one-way HMAC digests under an encrypted per-enrollment pepper with exact-one CAS consumption, online plaintext/encrypted legacy upgrade, malformed-envelope fail-closed behavior, and restartable lost-response enrollment. Disable, regeneration, activation, and replacement require exact factor proof, rotate trust/session generations, revoke sessions and trusted devices across database or secondary storage, reject stale TOTP proofs and sessions inserted after deletion, preserve the original absolute session expiry, and expose exact typed client/server contracts. Per-attempt lockout identities refund only their own internal-error reservation. Runtime two-factor passed 101/101; session, internal-adapter, and cookie-cache regression passed 127/127; product and required PostgreSQL passed 15/15; runtime/auth typechecks and builds passed; `git diff --check` passed; final strict rereview found no P0-P2. Next: digest-stored session and refresh families with rotation, reuse detection, and asymmetric access-token integration. |
| 2026-07-16 | Completed session, refresh-family, and OAuth credential authority | Persisted credentials expose only random handles and peppered digests; refresh rotation is atomic, lost-response-safe, and revokes a reused family; short-lived Ed25519 access tokens stay coupled to credential state; secondary storage carries a signed credential epoch; OIDC logout works before session materialization; and the `legacy-v1` to `digest-v1` fence drains, migrates, verifies, and rejects stale writers. Exact public `v0.2.1` artifact proof passed. Runtime passed 921 tests with 9 expected PostgreSQL skips; focused/broader regressions passed 207/207 and 465/465; database, API/CLI, and production-ops gates passed; three package typechecks/builds passed; package-surface proof passed 245/245 with a CLEAN independent Sonnet verdict at frozen SHA `5c4c1ce6e9631a650a245787225be7bdbab1bf34045de965003d9f59aa97847a`. Next: standards-complete passkeys. |
| 2026-07-16 | Froze the passkey security boundary and changed delegated-worker routing | All new delegated lanes use Claude Sonnet 5 under manager-authored, path-bounded prompts; the managing agent retains architecture, integration, verification, ledger, and commit ownership. Item 22 reuses Clearance's database-backed atomic challenge consumption and guarded adapter updates, requires configured origin/RP trust, mandatory user verification, unique credential IDs, discoverable login, redacted lifecycle views, counter compare-and-swap, and recovery-safe revocation. The first implementation slice is the runtime ceremony core; product wiring and destructive lifecycle remain separate reviewable slices. |
| 2026-07-16 | Closed the final Sonnet lane and changed future worker routing to Terra | The final read-only Sonnet audit was interrupted after it wrote outside its permission boundary and restored raw WebAuthn challenge persistence; that change was rejected and removed. Every new delegated engineering lane now uses `gpt-5.6-terra` at high reasoning with manager-authored bounded scope and targeted local verification. The managing Sol agent continues to own architecture, integration, builds, source-of-truth review, ledger updates, commits, and final judgment. |
| 2026-07-16 | Completed the passkey ceremony and product foundation (`abf3758`) | Clearance now ships default-on, conditionally typed passkey server/client APIs backed by `@simplewebauthn`; strict static RP/origin resolution; POST-based browser ceremonies; digest-only, typed, expiring, atomically consumed primary-database challenges with opportunistic cleanup; mandatory resident credentials and user verification; stable non-correlating user handles; discoverable authentication; globally unique credential IDs; redacted list/rename views; and counter compare-and-swap plus session creation in a required rollback-capable transaction. PostgreSQL fault injection proves a session created before a later failure rolls back together with the counter. Mongo installs verified remapped unique authorities for user handles, credentials, and challenge digests. Focused runtime passed 72/72, product 3/3, Mongo adapter 15/15, real PostgreSQL/MySQL/MariaDB/Mongo 4/4, rendered-browser Origin proof 1/1, runtime/auth/Mongo typechecks and runtime/auth builds passed, direct package import smoke passed, and the final Terra-high rereview was CLEAN. Item 22 stays open. Next: recent-proof passkey deletion and compromise revocation, last-viable-factor protection, and passkey session-generation invalidation. |
| 2026-07-16 | Accepted transactional passkey session-generation fencing (`0472a82`) | User and session rows now carry hidden passkey generations; DB-backed creation locks the user and initializes, backfills, hook-protects, revalidates, and inserts both passkey and two-factor generations in one rollback-capable transaction. Secondary-authoritative legacy sessions remain valid while both values are absent, then fail closed after lifecycle authority is established; malformed or mismatched generations, stale refresh rotation/recovery, direct ID reads, lists, and signed cookie-cache replay are rejected. Stale post-commit dual-write cache publication is skipped without misreporting an already committed DB session. Terra-high review found and drove closure of seven P1/P2 race, migration, rollback, and proof defects; manager verification caught and fixed one generic secondary-storage regression. Runtime typecheck passed, focused passkey/two-factor passed 27/27, the seven-file session/cookie/secondary/credential regression gate passed 289/289, runtime built, `git diff --check` passed, and the final Terra-high rereview was CLEAN. Item 22 stays open. Next: distinct-surviving-factor deletion proof, atomic credential removal, generation rotation, all-old-session revocation, and exactly one expiry-preserving replacement session. |
| 2026-07-16 | Completed recovery-safe passkey and factor lifecycle (`708647f`, `9f8a22e`) | Rollback-critical hooks now execute before commit and fail closed, custom mutations receive the ambient transaction adapter, and post-commit publication failures are aggregated without misreporting committed work. Passkey deletion, credential unlink, and TFA disable share authoritative-session checks, last-viable-factor protection, generation CAS, all-old-session revocation, and one expiry-preserving replacement session. Passkey deletion requires a distinct surviving factor; its digest-only challenge is consumed before ownership lookup and stays consumed across foreign/nonexistent targets, invalid proofs, and later lifecycle rollback. The legacy PostgreSQL bridge installs and validates exact columns, constraints, and indexes, rejecting partial, invalid, expression, included-column, unready, non-live, and `NULLS NOT DISTINCT` authorities with DDL rollback. Core passed 12/12, focused runtime 168/168, auth 41/41, real PostgreSQL bridge 10/10, cross-adapter PostgreSQL/MySQL/MariaDB/Mongo 5/5, core/runtime/auth typechecks and runtime/auth/sample builds passed, `git diff --check` passed, and the strict Terra-high rereview was CLEAN. Large legacy PostgreSQL tables require a maintenance window because bridge alterations and non-concurrent index creation can hold locks. Item 22 is complete. Next: Item 23 managed factor policy and account lockout. |
| 2026-07-16 | Froze the Item 23 dependency boundary | Current source proves concurrent durable second-factor lockout exists, while password sign-in has no shared budget across email, username, and phone; security configuration is startup-only and no managed policy authority reaches runtime replicas. Item 23 will land in four dependency-ordered units: credential-row password-attempt authority; durable scoped policy plus session assurance and restricted enrollment; product PostgreSQL authority and migration; then revisioned plan/apply/get/unlock API and CLI control. A `required` factor flag cannot ship before restricted enrollment and every session-issuance bypass are enforced. Unit 1 is active under a bounded Terra-high implementation contract; manager source review, builds, integration, and acceptance remain required. |
| 2026-07-17 | Completed shared password-attempt authority (`5eb30a3`) | Credential-account rows now own a default-on 10-attempt/15-minute password budget shared across email, username, and phone. Reservation CAS occurs before attacker-controlled hash work; concurrent success, failure, expiry, policy-threshold reduction, and password replacement settle without stale relock or old-password session issuance. Drizzle repeats the full guard on the outer PostgreSQL update. Hidden fields and exact legacy defaults fail closed. Manager proof passed focused runtime 158/158, product 10/10, legacy PostgreSQL 11/11, real Kysely PostgreSQL/MongoDB/MySQL/MariaDB plus Drizzle PostgreSQL 5/5, core/runtime/auth/Drizzle typechecks, five package builds, diff hygiene, and a CLEAN Terra-high rereview. Item 23 stays open; next is durable scoped policy, session assurance, and restricted enrollment. |
| 2026-07-17 | Froze the complete Unit 2 enforcement map | Every production session issuer converges on `internalAdapter.createSession`, while rotation/recovery, cookie cache, multi-session, one-time-token, organization switching, device exchange, impersonation, JWT, OIDC, and MCP can republish or derive authority afterward. Managed policy therefore binds an immutable product project/environment identity to an uncached scope-validating reader, accepts only runtime-derived evidence, stamps hook-proof session authority, and re-reads live policy before every issuance, publication, refresh, switch, or protected organization use. An unmet policy issues only a short-lived digest-stored, stage-bound enrollment/verification capability; it never creates a general session. Unit 2 owns the reader contract, assurance engine, restricted capability, derivative-path enforcement, and stale lock-reservation fencing with adversarial fake-reader proof. Unit 3 owns normalized product PostgreSQL tables, migrations, single-statement scoped reads, and revision concurrency. Unit 4 owns revisioned management/API/CLI policy and unlock workflows. Implementation starts with Unit 2A. |
| 2026-07-17 | Completed managed policy and session-assurance contracts (`79507d8`) | Unit 2A now provides one-time immutable project/environment binding, an uncached captured reader, exact same-read subject/organization membership attestation, canonical positive PostgreSQL-bigint revisions, strict plain-data policy and override normalization, recomputed effective policy, and opaque issuance contexts. Eleven runtime-owned session authority fields are migration-safe, hidden from public inference and output, protected from plugin/options replacement, and denied by generic create/update types. The assurance engine derives only from explicit evidence or a module-private immutable source snapshot; it enforces passkey, TOTP, OTP, recovery, impersonation, trusted-device, scope, organization, revision, freshness, causal timestamp, and expiry invariants without accepting caller labels or mutable `Date` authority. Manager verification passed core 12/12, focused runtime 65/65, core/runtime typechecks and builds, formatting, and diff hygiene; reciprocal Terra-high rereviews were CLEAN after closing membership, binding-mutation, prototype-injection, reserved-write, mutable-proof, device-cap, and stale-factor findings. Next: Unit 2B central issuance and live session-path enforcement. |
| 2026-07-17 | Completed central managed issuance and live session-path enforcement | Digest-only challenges now reserve and consume through exact adapter-native create-if-absent authority, and managed session creation accepts only opaque server-derived interactive, replacement, or impersonation evidence. Replacement issuance captures the live source session, credential lineage, subject, organization, assurance, and absolute expiry inside the exact transaction; centrally requires source retirement; prevents hook-based lifetime extension; and revalidates live policy before create, read, rotation, recovery, update, cache, publication, and commit. Every completed replacement caller passes captured authority. Unit 2C staged factor completion and Unit 2D device and derivative paths remain deliberately fail closed until their stronger evidence contracts land. Manager proof passed all ten affected typechecks, core 387/387, runtime affected 706/706 plus focused authority 152/152, SSO 432/432, auth 42/42, and memory/Kysely/Drizzle/Prisma/Mongo adapter suites 22/22, 5/5, 23/23, 17/17, and 16/16. Runtime and auth builds passed; diff hygiene passed; two independent Terra-high hostile rereviews were CLEAN after source-retirement, expiry-cap, orphan-credential, and precommit rotation/recovery policy findings were closed. Next: Unit 2C digest-only staged enrollment and verification capability. |
| 2026-07-17 | Froze the Unit 2C recovery boundary before integration acceptance | Normal staged passkey/TOTP authority is structurally session-capable only after the exact allowed factor proof and same-revision policy reread. Recovery remains fail closed and cannot create a managed session, but the promised durable repair workflow is still missing. Unit 2C therefore remains open until a separate digest-only recovery-repair authority consumes one-way recovery evidence, repairs a lost passkey or TOTP without issuing any session or derivative credential, revokes stale factor/session generations, and requires a fresh normal primary plus repaired-factor proof afterward. |
| 2026-07-17 | Completed sessionless passkey and TOTP recovery repair | Commits `f5468bc` through `18b1d8d`, `d4c9429`, `23f3636`, `3e26462`, `8d109e6`, `0f273b9` through `88f58d1`, and `3874319` through `2b591c6` add exact recovery-factor lineage, atomic dual generation rotation, one-way backup-code authority, an irreversible recovery-only bridge, isolated WebAuthn ceremonies, bounded passkey retries, custom-table TOTP replacement, disjoint fresh backup codes, mandatory durable lockout, exact source/user/policy/trust checks, deadline-preserving recovery-only retries, universal private generation schema authority, in-transaction ordinary-registration session revalidation, and one atomic null-portable TOTP activation/counter CAS. Active legacy TOTP authority remains exact: `verified === true`, or `verified == null` only while the user marker remains enabled. Concurrent commit `cde4493` weakened that boundary and was fully neutralized by `3e388f9` through `903b7b4`; the corrected tree matches its pre-regression parent for all nine affected files, and the passkey remediation predicate was separately restored. Manager proof passed core 15/15, the Unit 2C runtime gate 42/42, the correction gate 58/58, core/runtime typechecks, and diff hygiene. Terra-high hostile rereviews were CLEAN after schema ownership, invalid-proof replay, partial-counter commit, stale-marker authority, and passkey remediation findings were closed. Unit 2C is complete. Next: Unit 2D exact organization binding and derivative-credential enforcement. |
| 2026-07-17 | Accepted the Unit 2D central organization-transition authority slice | Commits `31f2d29` through `0769ce4` add an opaque one-use organization issuance context captured only inside the source transaction, exact source session and credential retirement, live target-membership and effective-policy resolution at issuance and precommit, organization-to-organization and null transitions, source evidence and expiry ceilings, exact target assurance and `activeOrganizationId`, unconditional `activeTeamId` clearing after hooks and overrides, direct-update denial, rollback on membership/revision races, and one-successor concurrency. Manager proof passed the focused authority gate at 212/212, core/runtime typechecks, and diff hygiene; Terra-high hostile review was CLEAN. Unit 2D remains open until every organization workflow, multi-session and one-time-token path, device authorization, impersonation, JWT, OIDC, and MCP derivative credential uses the central authority. Next: migrate organization activation, invitation, removal, leave, delete, and lost-membership workflows with final-cookie publication only after verified transition. |
| 2026-07-17 | Bound OIDC and MCP consent to authoritative subjects (`8a41f0a`) | The shared consent endpoint now re-reads stateful session authority, safely validates the complete authorization-code record, and binds its exact subject before revealing expiry or changing cookies, verification authority, consent rows, or code identifiers. Cross-user accept and deny attempts through body and signed-cookie lookup, malformed and null records, invalid or empty subjects, correct-subject malformed records, expired foreign records, and revoked-but-cookie-cached sessions all fail with non-disclosing errors while leaving the victim authority intact; the rightful subject can still consume the same code exactly once. Manager proof passed runtime typecheck, the focused OIDC matrix at 14/14, the shared MCP flow at 1/1, and diff hygiene after a Terra-high hostile review drove closure of cached-session, expiry-oracle, and malformed-record findings. Unit 2D remains open. Next: finish production organization lifecycle integration, then bind authorization-code exchange, refresh, userinfo, device, impersonation, JWT, and MCP derivative authority to live source policy. |

| 2026-07-17 | Made retried transactions hook-safe across core and Mongo (`dc5982e` through `ab1044b`) | Core transaction hook queues are isolated per whole-transaction attempt, so abandoned Mongo retries cannot publish duplicate or rolled-back after-hooks; only the committed attempt owns post-commit work. Mongo now delegates transient full-callback and uncertain-commit retries to `session.withTransaction`, preserves exact nested transaction identity, and always closes the session. Manager proof passed core 18/18, Mongo 20/20, both package typechecks, diff hygiene, and a CLEAN Sol-high strict review. Unit 2D remains open; next finish the organization authority integration and its combined runtime gate. |
| 2026-07-18 | Completed production organization lifecycle authority integration (`e9a3c50` through `f62f38c`) | Organization activation, creation, update, deletion, invitation acceptance/rejection/cancellation, member removal/leave, team admission/deletion, lost-membership repair, and lifecycle hooks now share exact transaction ownership, deterministic organization-first locking, live actor/session/membership/policy revalidation, guarded state transitions, atomic successor publication, fail-closed secondary-storage handling, exact last-owner/last-team invariants, and post-commit delivery/cookie behavior. Concurrent scope changes cannot revoke a valid successor or preserve stale authority. Manager proof passed the affected four-file gate 145/145, final integrated runtime gate 397 passed with one optional PostgreSQL skip and one todo, runtime typecheck, and diff hygiene; the only integrated review finding was repaired by moving invitation organization locks before live-session reads, and its narrow rereview was CLEAN. Round optimization consolidated three overlapping repair lanes into one six-file unit, canceled a redundant team-only review, reused unchanged proof, and ran no builds. Unit 2D remains open; next parallelize device/impersonation and JWT/OIDC/MCP derivative authority against the stable central session contract. |
| 2026-07-18 | Accepted the central live session-derivative authority (`69b734d` through `3f67022`) | Managed device, impersonation, JWT, OIDC, and MCP credentials now have one internal capture/validation primitive: an exact transaction captures strict live session lineage, subject, organization, absolute expiry, project, environment, and policy revision by bearer or stable session ID; a canonical versioned HMAC binding persists no bearer, credential secret/digest, mutable date, or process-local state; current, historical, and migrated legacy signing secrets work across restarts and nodes; validation authenticates before lookup and then exact-matches uncached live authority, so same-session credential rotation remains valid while tampering, revocation, expiry, policy or membership drift, security-generation changes, malformed lineage, and duplicate active credentials fail closed. Core transaction contexts and cross-owner views now share a live lifecycle that deactivates and unregisters on every commit, rollback, and retry attempt, preventing detached continuations from retaining a committed adapter. The first adversarial review found unauthenticated-binding and escaped-transaction HIGHs; both were repaired and the narrow rereview was CLEAN. Manager proof passed the source-linked runtime gate 191/191, core transaction gate 19/19, core/runtime typechecks, and diff hygiene. Round optimization built one primitive for five protocols, repaired the two independent findings in parallel, stopped a stalled transaction worker and integrated the minimal central fix directly, reused the repository's existing `dev-source` test configuration instead of building packages, and added only one regression per new invariant. Unit 2D remains open; next add derivative-aware device and impersonation issuance contexts, then integrate the five disjoint protocol paths against this accepted contract. |
| 2026-07-18 | Accepted derivative-aware issuance and production admin impersonation (`bdddc04` through `7d71032`) | Device issuance now requires a one-use captured context whose authenticated derivative binding exact-matches source subject and organization; impersonation requires exactly one server-derived `admin_impersonation` proof and a live source session. Both cap successor expiry to the source, overwrite hook/default/override attempts against organization, team, actor, and lifetime authority, revalidate source policy and lineage at precommit, roll back on hook-time drift, preserve the source session, and reject forged managed `impersonatedBy` fields. The production admin endpoint locks actor and target users in deterministic order, re-reads the authoritative actor session, evaluates live roles and permissions, captures and creates inside one transaction, and publishes admin/impersonation cookies only after commit; unmanaged behavior remains available. The first strict review found that the stronger context deliberately invalidated the old direct admin caller; integrating that caller closed the regression and the narrow rereview was CLEAN. Manager proof passed the central gate at 204/204, admin at 96/96, runtime typecheck, and diff hygiene. Round optimization kept the shared source/precommit rules in one issuance core, repaired the only production caller before committing, reused existing unmanaged admin coverage, ran one focused regression for source drift, and ran no builds. Unit 2D remains open; device route hardening and JWT integration are active, followed by the shared OIDC/MCP token path. |
| 2026-07-18 | Bound the RFC 8628 device flow to live source authority (`9428f2f` through `e871ae5`) | Managed approval now bypasses cookie-cache authority, captures the exact approving session inside its transaction, and atomically persists the authenticated derivative binding with exact user and nullable organization. Redemption claims only an unexpired approved row and creates the device session in the same rollback boundary; exact subject/organization validation, source-expiry capping, hook-safe issuance, missing/tampered/stale-binding rejection, source preservation, and one-successor semantics all come from the shared issuance core. Poll throttling uses exact observed-timestamp compare-and-set, and approval versus denial uses authoritative managed sessions plus exact pending/user compare-and-set so only one transition wins. Managed legacy rows without bindings fail closed while unmanaged rows retain their prior session path. The first strict review found claim-time expiry, cached denial, approve/deny race, and concurrent polling gaps; all were repaired and the rereview was CLEAN. Manager proof passed device 43/43, runtime typecheck, and diff hygiene. Round optimization extended two existing concurrency tests instead of adding new cases, reused central tamper/rollback proof, committed three disjoint files, and ran no build. Unit 2D remains open; JWT is active, then OIDC/MCP share one token-row integration lane. |
| 2026-07-18 | Bound every session-derived JWT to live source authority (`9a03d52` through `59849c8`) | POST rotation, deprecated GET issuance, and the legacy get-session response header now reload the strict source session, capture and validate purpose-separated authority, build custom/default claims from the live user, overwrite reserved binding/subject/organization claims, and sign with source-capped expiry inside the exact transaction. Custom `sub` remains application-controlled without becoming internal subject authority. Verification authenticates the JWT first, then exact-validates the live source and rejects stale, revoked, expired, policy-drifted, malformed, partially bound, or overlong tokens; explicit server-only generic JWTs remain compatible. POST rotation and signing are atomic and publish the successor cookie only after commit. The manager gate exposed a root-adapter JWKS deadlock that typecheck could not detect; signing now routes default and custom JWKS reads/creation through the ambient adapter. Strict review then found remote signers could strip binding claims and custom JWKS reads still received root context; both were repaired, remote output is consistency-checked before commit, and the rereview was CLEAN. Manager proof passed JWT 112/112, runtime typecheck, and diff hygiene. Round optimization stopped a stalled first worker, reproduced one canonical endpoint instead of debugging the whole matrix, fixed the shared adapter boundary once, added one remote-stripping rollback case, and ran no build. Unit 2D remains open; OIDC/MCP code, token, refresh, and userinfo integration is active. |
| 2026-07-18 | Bound OIDC and MCP token families to live source authority (`9bb700f` through `b6f65dd`) | OIDC and MCP authorization now capture purpose-separated live session authority and persist exact subject and nullable organization on single-use codes inside managed transactions. Consent revalidates the authoritative subject and binding, atomically consumes and reissues the challenge, caps the code to source expiry, and publishes cookie/output state only after commit. Code exchange consumes the exact challenge, validates source authority, locks the active user, source-caps access, refresh, and OIDC ID tokens, and persists binding authority before publishing credentials. Refresh propagates and revalidates the binding; userinfo and MCP get-session validate it transactionally; stale, tampered, missing, cross-purpose, or organization-drifted authority revokes the exact family, while transient policy-reader or database failures roll back without destroying valid credentials. Same-session credential rotation remains valid, managed legacy rows fail closed, and unmanaged protocol behavior remains compatible. Strict review found missing-authority fail-open paths, consent incompatibility, remote JWT expiry extension, root-adapter transaction reads, rollback header leakage, masked stale-surface tests, consumed-cookie test setup, and outage-triggered family revocation; every finding was repaired and the final settled-tree verdict was CLEAN. Manager proof passed the combined OIDC/MCP gate 106/106, runtime typecheck, and diff hygiene. Round optimization centralized authority capture, typed parsing, expiry calculation, and rollback-safe header restoration; issued independent proof families before credential rotation; replaced fragile middleware timing with one direct exact header-restoration test; stopped a stalled four-line repair lane and integrated it directly; and ran no build. Unit 2D now needs only its integrated derivative-path gate before closure. |
| 2026-07-18 | Completed Unit 2 managed runtime policy and derivative enforcement (`91cf95e` through `ff30653`) | The integrated gate now proves exact organization binding, multi-session selection, one-time-token redemption, device exchange, admin impersonation, JWT, OIDC, and MCP against the same live source-session and policy authority. The first run exposed that runtime tests were loading an ignored stale core build, then revealed two production consumers still coupled to the transaction store's retired private `isTransactionActive` field. Core now exposes lifecycle-aware exact-owner and rollback-capability queries; direct API dispatch and organization team admission use those public contracts, detached continuations remain inactive, exact nested owners are reused, and sequential transaction fallback remains non-atomic. Core rebuild was limited to refreshing the invalidated local dependency artifact. Focused proof passed core transaction 19/19 and the previously failing organization surface 117/118 with one intentional skip; the final serial affected-surface gate passed 826/827 with one intentional skip, core/runtime typechecks and diff hygiene passed, and the Terra-high hostile review was CLEAN. Round optimization converted private store inspection into two reusable queries, diagnosed the stale artifact with one exact reproducer, serialized only the shared-state integration gate, reused all accepted unit proof, added no duplicate tests, and ran no unrelated build. Unit 2 is complete. Next: Unit 3 normalized product PostgreSQL policy authority and verified migration. |
| 2026-07-18 | Froze the Unit 3 PostgreSQL authority contract | A policy-enabled product explicitly supplies immutable `projectId` and `environmentId`; delivery configuration, environment lookup inside the auth package, request headers, and active organization never infer that identity. Two normalized tables own the environment policy and sparse organization overrides under composite scope keys, positive monotonic bigint revisions, typed leaf columns, cross-field constraints, and an explicit presence bit for nullable assurance age. First migration seeds revision 1 from the resolved product security defaults without later startup overwrite: single-factor minimum assurance, currently enabled TOTP/passkey methods, current lockout/trusted-device settings, and no assurance-age ceiling. Every uncached read resolves the exact subject, optional membership, environment policy, and override in one PostgreSQL statement, uses the ambient raw transaction query when supplied, recomputes the effective policy, and fails closed on absent subject/scope/membership, malformed or regressed revision, schema collision, or database outage. Migration planning must expose the tables, fields, security marker, and SQL; application runs under an advisory lock, is idempotent, verifies the exact catalog plus seed, and rejects partial/incompatible authority without replacing data. Unit 4 owns revision-CAS writes and unlock workflows. Optimization: one authority module will own catalog inspection, DDL, seed, row decoding, and the future mutation seam, avoiding separate runtime, management, API, and CLI policy stores. |
| 2026-07-18 | Completed Unit 3 normalized product policy authority (`de40891` through `af94033`) | Two exact normalized PostgreSQL tables now own revisioned environment policy and sparse organization overrides. Migration planning reports 2 tables, 36 fields, the security marker, and deterministic DDL; apply is advisory-locked, seed-once, idempotent, rollback-safe, and exact-catalog verified. Runtime and migration configs capture the same immutable explicit scope; legacy credential generation is rejected, management never infers a fallback scope, current product defaults seed without later overwrite, and the credential fence cannot publish `digest-live` before policy migration succeeds. Every policy resolution remains one uncached statement, uses the ambient transaction when present, validates exact permanent tables, columns, defaults, named validated constraints, subject, optional organization membership, override, scope, and revision in that statement, and fails closed on collision or outage. Product proof held the policy lock and observed the fence remain `migrating`, then migrated, issued a session stamped with the exact scope/revision, and rejected later issuance after authority loss without creating another session. Focused auth proof passed 17/17, management scope proof 1/1, auth/management typechecks and declaration generation passed, diff hygiene passed, and the final strict review was CLEAN. Round optimization used one authority module and one integrated product proof, preserved the accepted runtime proof, regenerated only the invalidated declaration artifact, ran no package build, and deferred broad CI. Next: Unit 4 revisioned get/plan/apply/unlock API and CLI workflows. |
| 2026-07-18 | Froze the accelerated Unit 4 vertical-slice contract | The product authority will expose one transaction-capable semantic facade for exact get, preview-only plan, revision-CAS apply, redacted unlock preview, and confirmed unlock. Environment writes lock and increment the environment revision exactly once; sparse organization upsert/delete shares that revision; no-ops do not increment; stale writers fail without partial state. Unlock locks the exact user and deterministically clears only password and factor failure counters, reservations, and lock timestamps. Management will compose those mutations with the existing coordinated PostgreSQL audit transaction; the canonical operation registry then drives thin scoped API and stable JSON CLI adapters with preview-by-default confirmation. Acceleration: stopped the broad read-only map at diminishing returns, split the settled contract into non-overlapping authority and adapter lanes, centralized validation/SQL in the existing authority, and reserved one focused PostgreSQL invariant test plus contract/type proof instead of duplicating service, route, and CLI suites. |
| 2026-07-18 | Completed Unit 4 revisioned policy and unlock control (`6f3dea0` through `617309a`) | The optional product facade exposes immutable scope plus exact get/plan/apply/planUnlock/unlock. Environment and sparse organization writes lock in deterministic order, use canonical positive bigint compare-and-swap, increment the environment revision once only for real changes, couple override revision to the new environment revision, reject overflow and concurrent stale writers, and preserve no-op revisions. Exact-user unlock previews reveal counts only and confirmed unlock clears only password/factor counters, reservations, and lock timestamps. Management composes confirmed changes and redacted audit in one existing PostgreSQL transaction; four canonical operations provide authenticated scope enforcement, stable errors, explicit expected revisions, preview-by-default confirmation, API parity, and JSON/file-based CLI automation. Manager proof passed the real PostgreSQL authority 7/7, operation contracts 12/12, all four affected package typechecks, declaration generation, invalidated management/CLI artifact builds, real CLI help inspection, and diff hygiene. A broader pre-existing parity test still rejects the already-shipped schema-local `DATABASE_URL` help text and was not changed. Round optimization ended exploration early, ran authority and adapter work concurrently, reused the coordinated audit seam and operation registry, added one integrated PostgreSQL case plus one registry case, and avoided API/CLI mock-suite duplication. Next: purpose-separated credential keys and AWS/GCP KMS providers. |
| 2026-07-18 | Froze accelerated Unit 5 key-management contract | Source inspection found three product gaps with direct acceptance value: OIDC client secrets and SCIM bearer-token bases call `encryptRuntimeCredential(..., options.secret)`, while JWT private JWKs use the general runtime `secretConfig`. Tesseral uses six separately configured KMS instances and decrypts session private keys into process memory before software signing. Clearance will exceed that boundary with one reusable package and exactly three closed purposes: `oidc-client-secret`, `scim-bearer-token`, and `access-token-signing-key`. Every envelope binds provider identity, key identity, purpose, immutable project/environment scope, and resource identity; local AES-256-GCM, AWS KMS encryption context, and GCP additional authenticated data share one async seal/open/readiness contract. Strict production rejects implicit fallback and cross-purpose key identity reuse; development gets explicit HKDF-derived local parity. Legacy general-secret ciphertext remains read-only compatibility input and is rewritten under the active provider before old authority can retire. A separate asymmetric signer contract will support local, AWS, and GCP signing with public JWKs and retained verification keys, making plaintext private-key recovery unnecessary for cloud signing. Redacted status/readiness and preview-first migration/rotation flow through the existing operation registry, API, and CLI. Acceleration: build one package consumed by runtime, product, and management; keep cloud SDK calls behind injected clients so provider semantics need one deterministic contract gate; migrate only the three source-proven product domains; add one legacy migration integration case and one remote-signing/JWKS case; defer live cloud credentials, broad CI, builds, and release rehearsal to the frozen-candidate gate. |
| 2026-07-18 | Completed Unit 5A purpose-bound key providers (`e6e6aaf` through `e6a0ce0`) | New publishable `@clearance/key-management` owns a closed three-purpose provider contract, canonical versioned envelopes, exact project/environment/resource authority, stable redacted errors, rotation-aware retained reads, hashed readiness identities, and nonforgeable package-private separation metadata. The registry requires all three purposes, permanently binds each provider to its slot, compares copied local bytes in constant time, rejects cloud key-reference reuse, and exposes no secret-derived verifier. Local keys use exact 32-byte material and AES-256-GCM with bounded plaintext/ciphertext, random 96-bit nonces, and canonical AAD. AWS uses the official KMS client, immutable key ARNs/raw IDs with alias rejection, `SYMMETRIC_DEFAULT`, exact EncryptionContext, bounded aborts, enabled symmetric-key readiness, HTTPS endpoints with explicit non-production loopback-only HTTP, and injected-client parity. GCP uses the official Cloud KMS client, exact additional authenticated data, bounded calls, enabled-primary readiness, and verifies the returned CryptoKeyVersion belongs to the configured CryptoKey. Manager proof passed the focused provider contract 7/7, package typecheck, publish build, dependency installation including approved protobuf generation, and diff hygiene. The first hostile review found purpose bypass, forgeable key metadata, decrypt-size, redaction, validation, and canonical-authority gaps; all were repaired. Cloud review found real GCP response-name, AWS plaintext-HTTP, and mutable-alias blockers; all were repaired and the final Terra-high rereview was CLEAN. Round optimization stopped two pre-edit worker stalls at the visible-output deadline, integrated the settled local/GCP primitives directly, kept AWS as one bounded file lane, used one injected-client contract file instead of cloud mock matrices, ran one necessary new-package build, and deferred live-account proof to the release credential gate. Next: Unit 5B product migration for OIDC, SCIM, and stored JWT private keys. |
| 2026-07-18 | Completed Unit 5B purpose-separated product migration (`4e127d6` through `a8a219d`) | OIDC client secrets, SCIM bearer-token bases, and stored access-token private JWKs now use the closed purpose registry with immutable project/environment and hashed exact provider, organization, or public-key resource authority. Strict production requires explicit local/AWS/GCP provider configuration; development derives three HKDF-separated local keys. SSO and SCIM callback contracts carry stable validated authority, the runtime private-JWK seam binds protection to the exact persisted public JWK, and configured storage failures never fall back to the general secret. One advisory-locked PostgreSQL migration validates readiness, rewrites legacy general-secret ciphertext atomically, destroys retired private JWK material, normalizes envelopes, rejects marked malformed data, and is byte-idempotent. The management bridge loads exact environment configuration and creates/reconciles OIDC and SCIM material through the same product facade. Manager proof passed five affected typechecks, product 11/11, focused OIDC 1/1, SCIM 1/1, JWT 113/113, and a real PostgreSQL rollback/success/cross-resource/idempotence case; the adversarial review found and repaired new-marker legacy fallback, and the narrow rereview was CLEAN. Round optimization ran SCIM and JWT contracts concurrently after one resource-identity design, reused Unit 5A provider proof, consolidated three migrations into one transaction and one PostgreSQL case, and ran no builds. Next: Unit 5C remote asymmetric signing and retained JWKS publication. |

| 2026-07-18 | Closed the Unit 5B post-migration write boundary (`9afaa5a`, `b73a062`, `006540c`) | OIDC plaintext that resembles the historical wrapper is always encrypted instead of bypassing storage. The successful key migration installs PostgreSQL triggers in the same transaction so later product, plugin, or stale-process writes cannot reintroduce general-secret OIDC/SCIM ciphertext or recoverable retired/private JWK material. The real PostgreSQL proof stages legacy rows only by dropping the prior fences, proves final-stage failure rolls every earlier rewrite back, then proves migration reinstalls all three fences and each rejects a legacy write with SQLSTATE 23514. Focused SSO and product typechecks, the OIDC regression, the integrated PostgreSQL case, and diff hygiene passed. Next: Unit 5C remote asymmetric signing. |
| 2026-07-18 | Completed Unit 5C remote ES256 signing and retained JWKS (`04d9cee` through `9e9762c`) | One ES256/P-256 contract now supports deterministic local software signing, AWS KMS digest signing, and GCP KMS digest signing with exact response identity, CRC32C integrity, DER-to-JOSE conversion, immutable cloud references, hashed public `kid` values, redacted fresh readiness, and bounded retained keys. Strict production requires an explicit signer; development derives a fourth purpose-separated local key. Product token issuance reuses the existing live source-session, five-minute expiry, and transaction-bound custom-signing authority. The existing `/jwks` endpoint publishes cached immutable current/retained provider public keys plus a public-only, grace-bounded legacy query; it never generates or loads a new private signing key. OIDC and SCIM storage generations now use internal product-owned writer brands, monotonic revisions, batched row-locked migration, and database fences, so arbitrary custom callbacks cannot claim Clearance migration provenance. Management parses exact redacted local/AWS/GCP signer configuration. Manager proof passed key-management 10/10, runtime custom JWKS 1/1, SSO 35/35, SCIM 22/22, product 11/11, real PostgreSQL signing/migration 3/3, six package typechecks, provider-config construction for all three kinds, and diff hygiene. Strict review found and repaired GCP missing-name acceptance plus false generic-callback provenance; the narrow rereview was CLEAN. Round optimization ran the three provider files in parallel behind one frozen contract, reused the existing transaction/source-session signer seam and JWKS route, converted legacy JWKS access to one bounded public-only query, cached immutable cloud public keys while keeping readiness fresh, ran one integrated affected-surface gate, and deferred live cloud credentials, broad CI, and release rehearsal to the frozen-candidate gate. Next: Unit 5D atomic key status, migration, and rotation operations with exact API/CLI parity. |
| 2026-07-18 | Froze the accelerated Unit 5D operations contract | Exactly three canonical workflows will ship: read-only `key_management.status`, read-only `key_management.plan`, and confirm-gated `key_management.apply`. Status combines redacted encryption/signing readiness, hashed current/retained rotation identities, ES256 grace evidence, immutable scope, and exact migrated/pending counts without schema mutation. Plan returns the same migration authority plus a deterministic stale-plan token. Apply rechecks that token under the existing advisory lock, advances one bounded batch across OIDC, SCIM, and retired JWT storage, and appends its redacted audit fact inside the same coordinated PostgreSQL transaction; its result carries applied and remaining counts plus the next token so automation can repeat safely. API paths are `/v1/key-management/status`, `/v1/key-management/plan`, and `/v1/key-management/apply`; CLI paths are `key-management status`, `key-management plan`, and `key-management apply --expected-plan <token> --yes`. Acceleration: refactor the existing startup migrator into one reusable transaction-capable product primitive, keep management/API/CLI as thin adapters, parallelize only after the product and operation types compile, prove the invariant once in real PostgreSQL plus registry/transport parity, run no builds or broad suites, and reserve cloud, full CI, and release proof for the frozen `0.3.0` candidate. |
| 2026-07-18 | Completed Unit 5D atomic key-management operations (`d7d85ac` through `fc6427d`) | Product status is read-only and reports exact setup/migrated/pending state plus redacted encryption/signing readiness, public signing identities, algorithm, and grace. Plans bind immutable scope, exact next-row snapshots and counts, validated trigger/function authority, and the configured current encryption/signing write keys. Apply proves one explicit PostgreSQL transaction, rejects stale plans before writes, locks and revalidates the exact approved rows, advances at most five OIDC, five SCIM, and five JWKS rows, and returns actual counts. Management couples confirmed apply with a redacted audit event in the same coordinated transaction. Three canonical operations expose strict API routes and stable JSON CLI workflows; execution requires `dryRun:false` plus confirmation, while status/plan bypass credential migration and idempotency writes. Manager proof passed real PostgreSQL status/plan/stale/rollback/apply/completion 1/1, management registry 13/13, API 9/9, CLI 3/3, four affected typechecks, direct CLI help, and diff hygiene. Strict product review found and repaired write-key-independent plans, unenforced transaction capability, weak trigger readiness, and unstable provider errors; integrated adapter review found and repaired implicit execution plus two read-side middleware writes, and both rereviews were CLEAN. Round optimization reused one startup migration primitive and one coordinated audit seam, ran API and CLI in parallel after the registry froze, added one real database invariant case plus transport-only proofs, ran only the targeted management build required to refresh a stale local declaration artifact, and ran no broad suite or release build. Next: one append-only runtime authentication audit sink shared by login, recovery, factor, session, SSO, SCIM, API-key, invitation, webhook, and impersonation events. |
| 2026-07-18 | Froze the accelerated Unit 6 runtime-audit contract | Normal authentication handlers cannot safely join the management snapshot transaction. Runtime security mutations will instead append canonical redacted facts to a runtime-owned append-only outbox inside their existing PostgreSQL transaction, preserving lossless commit/rollback semantics without exposing raw SQL. One request context supplies generated or validated correlation, operation, route, actor, and minimized network metadata. Central session create/revoke/credential-rotation authorities cover most authentication paths; only factor, SSO, SCIM, invitation, asynchronous delivery, and impersonation terminal facts need narrow emitters. Existing normalized management events and their scoped list/tail/inspect/export API/CLI remain the sole operational surface, and existing API-key audit is reused rather than duplicated. Acceleration: deliver four dependency-ordered units, instrument transaction authorities instead of routes, prove rollback/redaction once in PostgreSQL, reuse existing transport contracts, and run no builds or broad suites until the frozen `0.3.0` candidate. |
| 2026-07-18 | Completed Unit 6A transaction-owned runtime audit foundation (`40c441b` through `c0e06dc`) | A private validated binding now appends canonical redacted facts only through the active PostgreSQL transaction. Product migration installs an exact append-only outbox with immutable update/delete/truncate guards, deterministic schema validation, scoped indexes, and fail-closed drift handling. One dispatcher context supplies validated or generated correlation plus bounded route, method, IP, and user-agent metadata. Central session creation emits committed `auth.session.created` and exact interactive `auth.login.succeeded`; credential-terminal failures emit anonymous, target-free `auth.login.failed` before arbitrary after hooks. One shared classifier covers password, OTP, magic link, passkey, social, anonymous, OAuth, and SSO callback templates without recording redirect initiations. Manager proof passed runtime/auth/management typechecks, runtime 3/3, real PostgreSQL commit/rollback/redaction/immutability/drift 1/1, generated public declarations, and diff hygiene. Terra-high review found and repaired false failure events after successful handler commits plus missing interactive route coverage; the final rereview was CLEAN. Round acceleration now overlaps 6B recovery/factor/session authorities with 6C protocol terminal emitters, starts 6D reader integration as soon as the stable outbox schema is imported, batches one final integrated review, and reuses the single PostgreSQL invariant proof rather than creating per-route test suites. Next: Units 6B and 6C in parallel, with 6D reader integration overlapping once event actions freeze. |
| 2026-07-18 | Completed Units 6B-6D and closed runtime auditability (`989b71b` through `8a904be`) | Session refresh rotation/recovery/reuse, revocation cause, passkey/TOTP/recovery, impersonation, invitation, OIDC/SAML, SCIM, and asynchronous webhook lifecycle transitions now append at their existing transaction authority. Delivery owns one reusable transaction-only primitive, exact append-only readiness, cursor indexes, and worker configuration aligned with the product schema/prefix. Operational event list/tail/inspect/export merge normalized management and runtime rows by stable created-at/id order without a principal or event cache; absent runtime storage is a bounded empty source. External provider IDs, credentials, email addresses, and URLs do not enter immutable metadata, and SCIM token actions use internal connection actors. Manager proof passed all affected package typechecks, worker config 10/10, auth runtime PostgreSQL 2/2, delivery store PostgreSQL 17/17, worker PostgreSQL 12/12, management event 8/8, runtime-reader PostgreSQL 1/1, and diff hygiene. One integrated Terra-high review found and repaired shared worker naming, duplicate OIDC failure facts, and external-provider metadata/actor leakage; both narrow rereviews were CLEAN. Round optimization froze one event contract, overlapped the disjoint runtime, protocol, delivery, and reader authorities, reused the existing API/CLI surface, consolidated redaction centrally, and limited new proof to transaction, delivery-transition, reader-ordering, and discovered-regression invariants. Next: close RBAC and tenant credentials through one normalized authorization authority shared by people and organization service accounts. |
| 2026-07-18 | Froze the accelerated Unit 7 authorization contract | One product-owned Postgres authority will replace snapshot custom-role arrays and comma-separated membership roles with scoped actions, roles, role-action edges, person/service-account assignments, service accounts, digest-only credentials, and one monotonic organization authorization revision. Effective-action reads are uncached, sorted, deduplicated, and derive project, environment, organization, subject, role, status, and action in one authority. Any role, action, assignment, service-account status, or credential mutation advances the organization revision; runtime authorization re-reads authority, while issued JWTs carry actions plus the revision and fail verification after a mismatch. Existing owner protection, API-key digest/expiry/rotation safety, operation-registry patterns, audit transactions, and environment scoping are reused. Four dependency-ordered units separate authority, reconciliation, runtime credentials, and surfaces. Acceleration: implement the shared authority once, overlap management and runtime consumers only after its types compile, defer UI until operations freeze, prove one real PostgreSQL union/revision/cross-scope case plus one runtime stale-claim rejection, and avoid redesigning role CRUD or duplicating secret machinery. Next: Unit 7A normalized authority and verified migration. |
| 2026-07-18 | Completed Units 7A-7B and the Unit 7C credential core (`1a74d9b` through `1bbf4fb`) | A product-owned seven-table PostgreSQL authority now seeds exact immutable owner/admin/member roles, validates catalog and scope constraints, provides uncached sorted effective reads, preserves the final owner, caps effective actions, and accepts only branded caller transactions. Management role and membership mutations, bounded one-organization reconciliation, management provisioning, and the product organization-create route commit normalized assignments and revisions with their existing runtime transaction. Session JWT issuance and verification bind sorted actions to live revision and reject generic, partial, stale, or authority-free claims. Service accounts now have many-to-many roles and 256-bit one-time credentials stored only as versioned SHA-256 digests with safe prefix/fingerprint; rotation, revocation, disable, wall-clock expiry, foreign-scope rejection, and revision invalidation are atomic. Manager proof passed authorization PostgreSQL 10/10, membership PostgreSQL 9/9, API roles 6/6, runtime organization/JWT 149/149, auth wrapper 11/11, affected typechecks, generated declarations, and diff hygiene. Strict Terra-high reviews found and repaired transaction/pool confusion, built-in minting, unbounded actions, claim acceptance without authority, database-collation drift, org-creation gaps, stale reconciliation, backend role routing/order, default bridge policy mismatch, credential cross-scope confusion, credential timing/status distinction, and transaction-start expiry. Round optimization froze the authority once, ran management, runtime organization, and credential consumers in parallel on disjoint files, used one focused proof at each authority boundary, and deferred builds/broad CI/UI to their actual gates. Next: issue and verify revision-bound machine tokens from the accepted credential result, then freeze the Unit 7D operation contract once. |
| 2026-07-18 | Froze the accelerated Unit 7D surface contract | Reuse existing role CRUD and add exactly twelve canonical workflows: effective inspect; assignment list/replace; bounded reconcile; service-account list/inspect/create/disable/enable; credential create/rotate/revoke. Organization scope stays in the existing nested API path and `--org` CLI convention. Replacement and reconciliation require server confirmation; disable, rotate, and revoke require client confirmation; creates and enable remain previewable. One-time secrets appear only on committed credential create/rotate and are stripped from idempotency replay. Management administration never exchanges a machine secret: the accepted server-only runtime JWT API authenticates it directly and issues a revision-bound token. Implementation order is registry plus bridge, API, CLI, surface registry, console, then Vault tenant self-service. Optimization: reuse one authority and the existing access-route/dispatch/console seams, freeze response shapes once, run API and CLI concurrently only after the registry compiles, and prove each invariant at its owning boundary. |
| 2026-07-18 | Ranked the post-Unit-7 acceleration path | First finish the existing store-v2 topology authority for projects, environments, and organizations as one parent-chain repository and one reversible cutover; its physical tables, verifier, checksum, and principal pattern already exist, and it stabilizes every later nested product query while closing normalized persistence. In parallel after Unit 7D contracts freeze: derive one runtime-validated browser/server client from the operation registry, complete SCIM Groups, and propagate OpenTelemetry through HTTP, PostgreSQL, jobs, and delivery. Then build Vault and the nested console on the shared topology, generated client, and accessible UI primitives so branding, domains, templates, and guided enterprise setup become workflows in one product shell rather than separate applications. Decisive proof stays one per kernel: parent-chain PostgreSQL cutover/rollback and scale; registry-to-API/CLI/client completeness; one authenticated browser journey; one scoped SCIM group lifecycle; one correlated redacted trace. Multi-architecture images, anonymous pulls, full Node CI, live SES-account evidence, release rehearsal, provenance, and registry proof remain terminal gates for the frozen `0.3.0` candidate. |
| 2026-07-18 | Completed Unit 7C machine-token enforcement (`65d19b1` through `6ab2e9a`) | The private runtime binding authenticates a digest-only service-account credential through the product authority and returns only exact organization, service-account subject, revision, sorted actions, and expiry. A server-only API issues the token without registering an HTTP route; its expiry cannot exceed credential expiry. Verification requires the complete machine claim grammar, rereads live service-account authorization, and rejects stale revision/actions, partial or mixed session claims, generic authority claims, absent or failed authority, and changed machine subjects from custom signers. Rotation, revocation, disable, assignment, or role/action mutation advances revision and invalidates an issued token immediately. Manager proof passed runtime/auth typechecks, JWT 115/115, product wrapper 11/11, and diff hygiene; the Terra-high strict review returned CLEAN after the manager added exact custom-signer subject preservation. Unit 7D operations and product surfaces are now the only RBAC gap. |
| 2026-07-18 | Completed Unit 7D1 canonical operations and management bridge (`2e84279` through `13f82b5`, `7e88ebd`) | The registry now has 125 unique typed operations, including four normalized-authorization and eight service-account workflows with exact nested API paths, CLI paths, decimal-string revisions, dry-run policy, and client-versus-server confirmation semantics. Thin management adapters validate exact scope and subject existence, reject orphan principals/service accounts, read the authority without an authorization cache, and execute assignment, reconciliation, account, status, and credential changes with redacted audit inside one coordinated PostgreSQL transaction. Preview-by-default assignment/reconciliation and all requested dry runs execute the real invariant path then deliberately roll back; public preview results contain only intent and current-state facts, never generated account/credential IDs, secrets, prefixes, fingerprints, or speculative revisions. Manager proof passed management typecheck, registry 14/14, and real-PostgreSQL bridge 10/10. Integrated Terra-high review found speculative preview shaping and subject-existence gaps; both were repaired, client-required confirmation was preserved at the CLI boundary by design, and the narrow rereview returned CLEAN. Next: API, CLI, and operator-console lanes in parallel against the frozen contract. |
| 2026-07-18 | Completed Unit 7D2 API/CLI and Unit 7D3 operator console (`24f838d` through `afbecce`) | All twelve frozen workflows now have exact nested API routes and stable noninteractive CLI commands. Request objects and optional fields fail closed; malformed JSON cannot become a live mutation; paired subject filters and canonical decimal revisions are enforced; server-required and client-required confirmations stay at their owning boundaries. Credential create/rotate responses carry no-store headers, and idempotency replay removes only the one-time secret while retaining safe metadata. The surface registry now covers role list/validate/create/update plus every authorization and service-account operation in both directions. The operator-only console adds accessible organization-scoped authorization and service-account workflows through the existing CSRF BFF, disables viewer mutations, scopes every async continuation and preview to its route version and organization, and never persists or caches one-time credentials. Manager proof passed API 17/17, credential replay 1/1, CLI transport 13/13, surface registry 4/4, console 67/67, syntax checks, and diff hygiene. One integrated Terra-high review found missing required role IDs, cross-organization UI races, and dropped secret-response cache controls; all were repaired and the final narrow rereview returned CLEAN. Acceleration kept API, CLI, and console on disjoint files after the operation contract froze, used one seam review, and began the non-overlapping topology kernel concurrently. Next: Unit 7D4 authenticated Vault tenant self-service; RBAC remains `In progress` until that boundary is real. |
| 2026-07-18 | Completed normalized topology authority kernel Unit T1 (`1c1cb84` through `f110b4f`) | Projects, environments, and organizations now have one reversible parent-chain authority with exact foreign keys, millisecond-normalized timestamps, stable bounded cursors, typed no-delete repositories, snapshot-authoritative shadow sync, empty arrays after cutover, exact reverse materialization, and a database guard against silent authority rollback. Direct writes do not lock or rewrite the snapshot and advance count/revision metadata with insertion deltas instead of per-mutation table scans; exact no-op upserts do not advance state. Manager review rejected the first snapshot-shaped cache/row-lock design, then found and removed a long-lived read-promise leak. The final proof passed management typecheck, one real-PostgreSQL parent-chain case covering cutover, all three cursor/idempotence/count paths, concurrent nonblocking writes, referential rollback, authority fencing, and reverse materialization, plus diff hygiene; the strict narrow rereview returned CLEAN. Round optimization now keeps the critical path on topology consumption: add one topology-plus-audit transaction seam, split core and auxiliary consumers only after that contract freezes, and reuse this migration proof while deferring 5k/50k measurement until the production path no longer touches snapshot topology. |
| 2026-07-18 | Completed topology transaction/publication seam Unit T2A (`feea2a1` through `b3723c4`, `d1cf4d4` through `dc63b96`) | Changed relational topology transactions now require an append-only audit fact in the same PostgreSQL commit; callback failure, forgotten issued work, audit failure, and authority mismatch roll back rows, state, and events together. Exact topology counts/revision are cached from durable metadata and refresh across processes without snapshot topology or count scans. One local publication chain orders snapshot, topology, principal, authority, and event projections after commit while database writers remain concurrent; monotonic snapshot/topology/principal clocks reject stale views, rejected committed candidates reload coherently, transaction clients are released before publication, and post-commit cache failures never falsify durable outcomes. Confirmed API and CLI cutover/rollback workflows are registered and transport-proven. Manager proof passed management typecheck, one focused real-PostgreSQL transaction/cutover/rollback/count/concurrency suite, operation/API/CLI focused gates, and diff hygiene; repeated strict review found and drove closure of stale-refresh, mixed-event, retention, post-commit outcome, and pool-starvation races, and the final verdict was CLEAN. Round optimization replaced race-specific cache patches with one publication invariant, froze the next reader contract from one read-only map, retained one decisive PostgreSQL proof, ran no builds, and split T2 into bounded reads then lifecycle writes so the core reader lane can start without another discovery round. |
| 2026-07-18 | Completed normalized topology core reads Unit T2B (`3e5b33d` through `cc36f08`) | Scoped project/environment/organization point reads, environment and organization lists, environment inspection, project overview, and their production API routes now select relational topology whenever it is authoritative; synchronous JSON behavior remains intact. Lists use limit-plus-one keyset SQL with surface-stamped environment/organization cursors, scoped active-organization counts are scalar SQL, cross-scope point reads remain indistinguishable from missing, and unpaginated legacy responses keep their compact shape unless a continuation exists. Membership, enterprise-connection, directory-connection, and event counts that cannot yet be derived from the remaining snapshot authority return explicit `null` instead of false zero; recent events use the bounded authoritative reader. Manager proof passed management typecheck, one focused post-cutover PostgreSQL consumer/cursor/count/event case, syntax/diff hygiene, and strict integrated review. The first review found false auxiliary/event counts, four synchronous organization prechecks, and response-shape drift; all were repaired and the final verdict was CLEAN. Round optimization implemented management and API consumers in parallel behind one frozen contract, added only one count query and one cursor surface, mapped lifecycle writes concurrently, ran no builds, and identified one reusable coordinated topology capability as the next bottleneck. |

### Current implementation decisions

- Store-v2 is default-off and Postgres-only. Explicit plan/apply/status/verify/cutover/rollback operations control adoption; events and principals can become independently relational-authoritative while projects, environments, and organizations remain verified shadows.
- Relational principal authority publishes a compact atomic `{revision,count}` state only at transaction finalization. Public callbacks receive frozen allowlisted capabilities without raw SQL or physical deletion; divergence fails verification without exposing PII.
- Audit and principal authority remove their snapshot rewrite costs and pass 5k/50k production gates. The persistence dimension stays `In progress` until projects, environments, and organizations complete equivalent reversible cutovers.
- Delivery uses Postgres-native immutable events, leased jobs, attempts, and worker heartbeats. Payloads and endpoint secrets are encrypted under a purpose-specific encryption keyring. Schema v3 records fingerprint key identity, preserves a separate stable dedupe authority across rotations, requires explicit legacy-key attribution for non-empty v1/v2 migration, and blocks workers from becoming ready while any queued/leased/retry destination or migrated legacy source authority references an unavailable key.
- Customer-managed webhook endpoints live in guarded delivery schema v4 rather than the management snapshot. URL plus signing secret use a purpose-derived encryption subkey and scope/version-bound AAD; public views omit envelopes and key IDs, create/rotate reveal a generated secret once, exact subscriptions start with `organization.updated`, URL changes disable the endpoint, and versioned lifecycle mutations fail closed on stale writes.
- Runtime verification, reset, and invitation email jobs use their existing relational transaction context. Management PgStore mutations can issue opaque delivery enqueues on the same transaction; `organization.updated` is the first canonical management event. Scoped delivery jobs and managed endpoint lifecycle are stable operation-registry resources with exact API/CLI parity.
- Delivery semantics are at-least-once with stable event IDs, deterministic SMTP Message-ID values, bounded full-jitter retries, and dead-letter state. Provider acceptance is checkpointed before final completion when the lease remains owned; the unavoidable crash boundary after provider acceptance remains an at-least-once ambiguity. Webhook signatures cover the exact canonical bytes plus event ID and timestamp; HTTPS, all-answer global-unicast validation, connection pinning, redirect refusal, bounded response handling, and destination-fingerprint verification fail closed. Retry/cancel/replay and endpoint lifecycle are preview-first, explicitly confirmed where destructive, scoped, audited, and available through API and CLI.
- Delivery-worker metrics use a fixed label vocabulary only: transport channel, bounded outcome, and configured email transport. Tenant, project, environment, endpoint, event, job, provider response, and error values never become metric labels. `/metrics` remains an internal operational surface alongside JSON `/live` and `/ready`.
- Runtime delivery uses a distinct durable enqueue capability. Signup uses its existing adapter transaction; reset and invitation state changes are wrapped with `runWithTransaction` and enqueue through `getCurrentAdapter`. Legacy background email callbacks and `queueAfterTransactionHook` are prohibited because they leave post-commit loss windows.
- Delivery persistence separates immutable event metadata, encrypted expiring payloads, jobs, append-only attempts, and worker heartbeats. Recipient, subject, body, URLs, reset/verification tokens, and invitation details remain inside a purpose-specific AES-256-GCM envelope; keyed fingerprints support quotas and deduplication without plaintext PII.
- Relational audit retention is logical: rows removed from the visible retained window are tombstoned with `visible = false`, preserving the append-only archive. Rollback materializes the visible projection into the snapshot and requires a fresh verified cutover before relational authority resumes.
- Authentication security defaults are product-owned: TOTP, passkey ceremony support, and five-minute ES256 access tokens are enabled unless explicitly disabled; HIBP is enabled in strict/production mode; OAuth provider tokens use versioned authenticated envelopes; and public API types reflect disabled or dynamically configured plugins. Factor enrollment is one-row and lost-response-safe, replacement requires the active factor and stages until proof of the new secret, TOTP timesteps are consumed atomically against exact factor generation, and identity-bearing account attempt reservations precede attacker-controlled comparisons. Recovery credentials use one-way digests with online legacy upgrade; disable and regeneration require exact TOTP or recovery proof and rotate storage-aware trust plus session generations. Session and OAuth credential authority persists only handles plus peppered digests, rotates refresh families atomically with lost-response recovery and reuse revocation, binds short-lived access tokens to current authority, signs secondary-storage epochs, and fences `legacy-v1` writers before verified `digest-v1` cutover. Legacy signing keys retain their first retirement timestamp so repeated migration cannot extend grace. Purpose-separated encryption, production provider configuration, atomic legacy migration, local/AWS/GCP remote signing, retained public JWKS, key operations, and transaction-owned runtime audit are complete.
- Passkey ceremonies use a reviewed WebAuthn library and Clearance's primary database for typed, short-lived, single-use challenges. Request headers never become RP or origin authority; discoverable credentials remain enumeration-resistant; credential identifiers, public keys, user handles, and counters stay internal; and nonzero signature counters advance through a guarded compare-and-swap before session issuance. Destructive passkey, password, and TFA lifecycle operations require an authoritative current session and a surviving factor, consume one-shot proof outside destructive rollback, rotate generation authority with compare-and-swap, revoke every old session, and issue exactly one replacement capped to the original expiry. Legacy PostgreSQL bridge DDL fails closed on incompatible column, constraint, or index authority; large installations must schedule its table alterations and non-concurrent index creation within a maintenance window.
- Managed authentication policy will use normalized, revisioned PostgreSQL authority scoped to Clearance project and environment, with optional organization overrides validated against that scope and uncached runtime reads. Password attempt authority now belongs to each credential account and is shared across email, username, and phone entry points; product defaults are enabled, 10 attempts, and 15 minutes, while direct runtime configuration is bounded and legacy state fails closed. Required-factor policy depends on durable session assurance and a restricted enrollment flow; policy activation cannot silently exempt unenrolled users or trust client-supplied assurance.
- Policy-enabled runtimes capture immutable project/environment identity separately from delivery configuration, environment variables, request headers, and active-session organization state. The internal reader must return the captured scope exactly, validate organization membership in the same authoritative read as an override, return canonical monotonic decimal revisions, and distinguish an explicit absent override from an unavailable authority. Missing, malformed, regressed, cross-scope, or unavailable authority fails closed without a process cache or last-known-good fallback.
- Managed session assurance separates verified authentication evidence from credential-lifecycle generations. Overrides, generic updates, database hooks, copied sessions, administrators, and external plugins cannot set or inherit assurance fields. Rotation and recovery preserve original authentication timestamps; factor age never refreshes accidentally. Policy changes, organization switches, cached sessions, multi-session selection, one-time-token redemption, device exchange, impersonation, JWT, OIDC, and MCP all revalidate live authority before use or derivation.
- Stateful and stateless session derivatives share one restart-safe internal authority binding. Its HMAC key version is explicit and rotation-aware; its signed body contains only stable source scope and expiry fields; every consumer must validate inside the exact transaction with expected purpose, subject, and organization before issuing, refreshing, verifying, or revealing a derivative credential. Detached asynchronous work cannot reuse a completed transaction context.
- A user who has not yet satisfied managed policy receives a short-lived, single-use, digest-stored, stage-bound pre-session capability for only the allowed passkey or TOTP enrollment/verification steps. No normal session, organization authorization, token derivation, or device approval exists until the final factor proof and a same-revision policy reread succeed. Recovery evidence can enter remediation but cannot satisfy a managed passkey or TOTP requirement for general access.
- New delegated implementation work uses `gpt-5.6-terra` at high reasoning. Terra receives manager-authored bounded contracts and runs targeted tests, lint, and typechecks for its own edits; it does not run builds or own acceptance. The managing Sol agent reviews the exact diff and independently verifies every accepted change.

### Terminal PR and `0.3.0` release gate

- [ ] Freeze all source, schema, package-version, deployment, and release-workflow changes.
- [ ] Confirm every dimension in this ledger is `Proven ahead` with direct evidence.
- [ ] Run targeted verification for each completed unit, then the exact local release rehearsal.
- [ ] Run the complete CI matrix under every supported Node version.
- [ ] Review the full branch diff for correctness, security, generated artifacts, prohibited references, and release consistency.
- [ ] Push the branch once and open a review-ready pull request describing the ledger, implementation, migrations, operational impact, and verification evidence.
- [ ] Resolve every actionable review thread and rerun only the proof invalidated by review changes.
- [ ] Create the immutable `v0.3.0` tag only after the candidate and guarded tag workflow are proven.
- [ ] Publish `0.3.0` through the tag-triggered release path with signed provenance, SBOMs, signatures, and exact tag-commit binding.
- [ ] Verify SHA-512 integrity and signed provenance from the public npm registry, retrying first-publish read-back for up to ten minutes.
- [ ] Verify anonymous installation and anonymous multi-architecture image/chart pulls by immutable digest.
- [ ] Attach final evidence to the release and mark the goal complete only after public visibility is confirmed.

## Comparison baseline

Downloaded Tesseral to [tesseral-reference](/Users/stephenwalker/Code/projects/tesseral-reference) and pinned the comparison to public `main` commit `de5d3348380b820b2670c96728d36e456f696538` from September 10, 2025. Clearance was inspected at `969d6d2901a4a45421cdf27e06caf4ad3b2ed553`, including the current uncommitted worktree.

## Executive verdict

Tesseral is currently the more complete auth product. Its lead comes from customer-facing workflows, a normalized service architecture, durable delivery infrastructure, and productized authentication security.

Clearance is already stronger as an open-source, operator-controlled platform. Its CLI, environment lifecycle, multi-organization identity model, deployment artifacts, recovery tooling, protocol rigor, and release supply chain are materially better.

| Area | Leader | Assessment |
|---|---|---|
| Hosted login and customer portal | Tesseral | Decisive |
| Console and administrative UX | Tesseral | Decisive |
| Database and async architecture | Tesseral | Decisive |
| MFA, passkeys, invitations, branding | Tesseral | Decisive |
| CLI and automation | Clearance | Decisive |
| Self-hosting, recovery, release safety | Clearance | Decisive |
| SAML and SCIM protocol rigor | Clearance | Meaningful |
| Environment and multi-org identity model | Clearance | Meaningful |
| SDK onboarding | Tesseral | Current advantage |
| Runtime/framework implementation breadth | Clearance | Latent advantage |

## Where Tesseral is better

1. **It ships the complete end-user product layer.**

   Tesseral’s Vault covers login, signup, recovery, account security, organization administration, invitations, API keys, audit logs, SAML, OIDC, and SCIM, including guided Google, Entra, and Okta setup flows ([Vault routes](/Users/stephenwalker/Code/projects/tesseral-reference/vault-ui/src/App.tsx:118)). Clearance currently exposes a basic sample application and generic SSO/SCIM setup capability links.

2. **Its operator console models the actual product.**

   Tesseral has nested pages for organizations, users, sessions, passkeys, roles, API keys, authentication policy, enterprise connections, branding, and domains ([console routes](/Users/stephenwalker/Code/projects/tesseral-reference/console/src/App.tsx:91)). Clearance’s console remains a small flat application centered on operational resource lists ([current routes](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-console/public/app.js:13)).

3. **Its core database is designed for scale and querying.**

   Tesseral uses normalized PostgreSQL tables, `sqlc`, and 95 numbered schema migrations. Clearance’s management service still stores global state in one JSONB row, serializes writes with `FOR UPDATE`, rewrites the snapshot, and rebuilds uniqueness tables on mutation ([snapshot store](/Users/stephenwalker/Code/projects/clearance-auth/packages/management/src/store/pg-store.ts:1)). The repository already documents the appropriate normalized replacement in [DESIGN-store-v2.md](/Users/stephenwalker/Code/projects/clearance-auth/DESIGN-store-v2.md:159).

4. **It has a durable delivery plane.**

   Tesseral transactionally enqueues verification, reset, invitation, and webhook jobs, then processes them through a separately deployed River worker. Clearance enables email/password without a configured verification or reset delivery implementation ([product factory](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/create-auth.ts:175)). There is currently no application worker, transactional outbox, webhook delivery, retry, dead-letter, or replay system.

5. **MFA, passkeys, RBAC, and invitations are finished workflows.**

   Tesseral persists WebAuthn credentials, TOTP authenticators, recovery codes, lockout state, invitations, many-to-many role assignments, and organization API-key roles. Clearance contains a latent two-factor plugin, while the product bundle only wires organization, SSO, and SCIM ([plugin construction](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/create-auth.ts:112)). Passkey implementation and invitation operations are absent from the shipped product surface.

6. **Its token and key architecture is more mature.**

   Tesseral stores refresh-token digests, issues five-minute asymmetric access tokens containing resolved actions, and protects rotating per-project signing keys with AWS/GCP KMS abstractions ([access-token issuance](/Users/stephenwalker/Code/projects/tesseral-reference/internal/common/store/accesstoken.go:25)). Clearance has capable JWT machinery in its runtime, though the product does not expose it as the canonical session/access-token architecture.

7. **Its application integration story is clearer.**

   Tesseral documents React, Next.js, Express, Python, Go, and Rust integrations. Clearance has broad framework and datastore code in-tree, while its public product types still include `any` and `unknown` surfaces ([public runtime types](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-auth/src/public-types/index.ts:51)) and framework onboarding remains thin.

## Clearance’s most serious gaps

- **Managed API keys currently do not authenticate management API calls.** `/v1` still uses the single `CLEARANCE_OPERATOR_TOKEN`. Created key digests and scopes are lifecycle metadata rather than an enforced authorization boundary.

- **Runtime identity activity is missing from the audit ledger.** Management mutations are recorded and redacted well; login, factor, session, SSO, SCIM, and recovery activity needs the same append-only audit sink.

- **Session compromise has a larger blast radius than necessary.** Opaque session values are stored directly. The optional bearer plugin accepts unsigned input by default. Move to token digests, short-lived asymmetric access tokens, refresh rotation, and reuse detection.

- **Custom RBAC is disconnected from application authorization.** Management stores permissions, while runtime membership receives a role slug and sessions lack resolved action claims.

- **Credential purpose separation is incomplete.** Runtime OIDC and SCIM credentials reuse the general auth secret. The existing management keyring is a good foundation for separated key domains and external KMS providers.

## Where Clearance should preserve its lead

- The CLI already covers projects, environments, users, organizations, sessions, roles, SSO, SCIM, diagnostics, migrations, backups, restores, and upgrades ([CLI commands](/Users/stephenwalker/Code/projects/clearance-auth/packages/clearance-cli/src/index.ts:93)). Tesseral’s CLI is primarily bootstrap and database migration tooling.

- Clearance explicitly models development, preview, and production environments. Tesseral lacks that dimension.

- Clearance separates principals from organization memberships, allowing one identity to belong to several organizations. Tesseral directly associates each user record with one organization.

- Clearance has production Compose, Helm, Terraform, NetworkPolicy, PDB, ServiceMonitor, backup scheduling, restore verification, and rollback guidance. Tesseral’s checked-in deployment stack is development-oriented.

- Clearance’s SAML correlation, replay prevention, destination validation, and default rejection of IdP-initiated login are stronger. Its SCIM implementation also has broader discovery, deprovisioning, and session-revocation behavior.

- Clearance’s signed release, provenance, registry-readback, backup, and recovery discipline is substantially stronger.

## Recommended closure sequence

1. **Normalize management persistence.** Execute store-v2 using expand/backfill, dual writes or equivalent compatibility, checksum verification, cutover, and CLI-visible migration status.

2. **Make credentials real authorization boundaries.** Authenticate managed API keys by digest, enforce project/environment/status/expiry/scopes, attribute audits to key IDs, and retain the operator token only for bootstrap or break-glass use.

3. **Add the durable worker plane.** Implement a transactional outbox, bounded retries, dead-letter state, signed webhooks, SES/SMTP email, and CLI commands for inspection, retry, cancellation, replay, and readiness.

4. **Finish the identity security product.** Productize the existing JWT, TOTP, and breached-password capabilities; add WebAuthn/passkeys, invitation lifecycle, factor policy, recovery, lockout, asymmetric access tokens, KMS providers, and runtime audit events.

5. **Ship `@clearance/vault`.** Cover hosted login/signup/recovery, organization switching, account security, invitations, customer-managed enterprise connections, API keys, and audit logs.

6. **Rebuild the console around typed nested resources.** Use the canonical operation registry to generate runtime schemas and browser/server clients. Preserve exact CLI equivalents for every mutation.

7. **Complete the ecosystem.** Add React/Next/Express guides first, then thin Python/Go/Rust verification libraries, OpenTelemetry tracing, branding/domains, and multi-architecture images.

The strongest direction is to retain Clearance’s CLI and operations model while adopting Tesseral’s product completeness, normalized persistence, worker architecture, and security workflow coverage.
