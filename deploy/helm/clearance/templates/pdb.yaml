{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-api
  labels:
    app.kubernetes.io/name: clearance
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: api
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      app: clearance-api
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
{{- if and .Values.delivery.enabled .Values.delivery.worker.podDisruptionBudget.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-delivery-worker
  labels:
    app.kubernetes.io/name: clearance
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: delivery-worker
spec:
  minAvailable: {{ .Values.delivery.worker.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      app: clearance-delivery-worker
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
{{- if and .Values.console.enabled .Values.console.podDisruptionBudget.enabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ .Release.Name }}-console
  labels:
    app.kubernetes.io/name: clearance
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: console
spec:
  minAvailable: {{ .Values.console.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      app: clearance-console
      app.kubernetes.io/instance: {{ .Release.Name }}
{{- end }}
