{
  "name": "@clearance/vault-host",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Production host for the Clearance Vault customer portal",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clearance/management": "workspace:*",
    "@clearance/vault": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "catalog:"
  }
}
