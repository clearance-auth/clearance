# `@clearance/call`

Typed endpoint, middleware, routing, and client primitives used by the Clearance runtime.

This is an internal runtime package. Application code should normally install `@clearance/auth` or `@clearance/runtime`.
