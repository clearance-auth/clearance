{
  "name": "@clearance/cli",
  "version": "0.2.0",
  "description": "Clearance CLI — operate auth projects, enterprise connections, migrations, and deploys",
  "type": "module",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/clearance-auth/clearance", "directory": "packages/clearance-cli" },
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "clearance": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
		"build": "tsc -p tsconfig.json && node scripts/stage-ops.mjs && chmod +x dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "vitest run --maxWorkers=1",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clearance/management": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
