/** Default origin exposed by the local Compose management API. */
export const DEFAULT_MANAGEMENT_API_ORIGIN = "http://localhost:13200";

/** Backwards-compatible name used by the interactive CLI entrypoint. */
export const DEFAULT_CLEARANCE_API_ORIGIN = DEFAULT_MANAGEMENT_API_ORIGIN;

export const DEFAULT_LOCAL_DOCTOR_TIMEOUT_MS = 2_000;
